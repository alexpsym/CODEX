Final implementation summary - OANDA export delivery and journal Git-sync safety

Publication status
- Work was performed only in C:\GPT\CODEX-master from the current local checkout.
- This summary is written before publication as required by AGENTS.md.
- PATCH_DESKTOP_PUSH.bat must run next. The task is not complete until it succeeds and verifies local HEAD equals origin/master.

1. Safe journal GitHub synchronization
- Removed state_backup.json from _repo_state_files_for_github(); .gitignore was not changed and no force-add behavior was introduced.
- Added a scoped Git eligibility check using git ls-files --cached --others --exclude-standard for only the explicit journal candidates.
- Ignored untracked candidates are excluded before staging, while tracked files and the tracked missing legacy journal deletion remain eligible.
- Git staging is path-scoped with git add -A -- <eligible journal paths>.
- Commits are path-scoped with git commit --only so unrelated pre-existing staged files are not swept into an automatic journal commit.
- An unchanged eligible journal set returns a successful no-op before remote lookup, staging, commit, or push.
- A genuinely empty source allowlist retains the existing NoEligibleFiles failure instead of falsely reporting success.
- Git availability, eligibility, status, add, diff, commit, and push failures remain explicit failures.

2. OANDA export delivery workflow
- When an export job becomes ready, the page immediately displays a persistent manual Download export file link.
- The automatic browser download is attempted before the OANDA Trading Journal backfill request begins.
- Status text now distinguishes export creation, the download attempt, journal updating, and the final outcome.
- A backfill failure no longer blocks automatic delivery of the already-created export and leaves the manual link visible.
- An automatic-download failure still permits OANDA backfill, reports both outcomes truthfully, and leaves the manual link visible.
- Combined automatic-download and backfill failures preserve both error outcomes and the manual link.
- Successful Bybit and CoinSpot flows still perform exactly one download and do not call the OANDA backfill endpoint.
- Server-side export retention behavior was not changed.

3. Backfill and rollback preservation
- The verified OANDA master-journal import, later non-trade account-balance provenance, workbook reread, forced snapshot rebuild, and duplicate/idempotent re-import behavior were not weakened.
- Genuine GitHub synchronization exceptions and returned enabled-sync failures still trigger local rollback.
- Remote state is not reported restored when a GitHub sync call raises or explicitly returns failure.
- Overall rollback_restored remains false unless both local and remote restoration are genuinely established.

4. Regression coverage
- Added a real temporary Git-repository fixture with ignored local state, unrelated staged/unstaged/untracked changes, a canonical journal change, and a local bare remote.
- Verified ignored state_backup.json is absent from github_sync_files, staging, commits, and the remote while the canonical journal change commits and pushes successfully.
- Verified unrelated staged, unstaged, untracked, and ignored files remain byte-for-byte unchanged and retain their original Git states.
- Verified an unchanged eligible journal set is a successful no-op without remote lookup, add, commit, or push.
- Verified a fully empty allowlist remains a NoEligibleFiles failure.
- Preserved focused coverage for the tracked missing legacy journal deletion.
- Added executable browser-flow tests for link timing, download-before-backfill ordering, OANDA success, download failure, backfill failure, combined failure, and non-OANDA isolation.
- Added rollback coverage for a returned enabled GitHub-sync failure after local row/workbook state was committed.

5. Verification
- tests/test_history_page_js_download.py: 9 passed.
- Git-sync/repository-state selection in tests/test_master_service_master_journal_sync.py: 9 passed, 114 deselected.
- tests/test_master_service_oanda_history_export.py: 44 passed.
- Final realistic later-financing OANDA plus raised/returned GitHub rollback spot rerun: 3 passed.
- python -m py_compile render/master_service.py: passed.
- node --check render/static/history_page.js: passed.
- git diff --check: passed with only normal LF-to-CRLF working-copy notices.
- No full suite and no real broker export were run.
- Pytest emitted existing FastAPI on_event deprecation warnings and could not update the repository pytest cache; all selected tests passed.

6. Safety and preservation
- journal/Trading Journal.xlsx had a pre-existing staged modification and was not modified by this task. Its SHA-256 remained exactly 8925B34088B24CB8415A936B8277E6C3F4ABF952A9B8302DB0BF359A05BFED29.
- Pre-existing unstaged changes in render/data/monthly_aud_revaluation_state.json, pending_webhooks.json, trade_contexts.json, and webhook_attempts.json were not edited for this task and must remain unpublished.
- No ignored local-state file, environment file, secret, log, cache, or runtime data is included in the intended commit.

Changed task files
- render/master_service.py
- render/static/history_page.js
- tests/test_history_page_js_download.py
- tests/test_master_service_master_journal_sync.py
- tests/test_master_service_trading_journal_crud.py
- summary/CODEX_SUMMARY_latest.txt

Mandatory completion step
- Run repository-root PATCH_DESKTOP_PUSH.bat.
- Do not report completion unless it succeeds and proves local HEAD equals origin/master.
