Final summary

Status:
- Cleaned `/api/open-orders` so public `items` contain only genuine broker exposure rows, while pending webhook reconciliation remains internal and visible in `source_counts`.
- Preserved pending webhook records with distinct stable IDs; fingerprint dedupe now applies only to idless pending records.
- Updated the Open Orders empty state to refer only to open orders/positions.
- Restored the current workbook's SYMBOLS manual header shape: Stops `AJ1:AM1`, Targets `AN1:AQ1`, vertical duration headers `AR:AV`, AT/AU/AV width 43, and AV as the final thick data boundary.
- Restored Trade Log merged-group right edges so `X1` and `AC1` reopen with medium right borders and no grey fill.
- Fixed SYMBOLS data-only resync to avoid blanket header rewrite/merge repair, using targeted duration-header repair instead.

Files changed intentionally:
- journal/Trading Journal.xlsx
- render/master_service.py
- tools/master_journal_workbook.py
- tests/test_open_orders_page.py
- tests/test_master_journal_workbook.py
- summary/CODEX_SUMMARY_latest.txt

Verification performed:
- `python -m py_compile render/master_service.py tools/master_journal_workbook.py`: passed.
- `node --check render/static/open_orders.js`: passed.
- `python -m pytest tests/test_open_orders_page.py -q --basetemp .pytest_tmp_open_orders_verify`: 23 passed.
- `python -m pytest tests/test_master_journal_workbook.py::test_772eedb_symbols_trade_log_formatting_migration_restores_manual_layout tests/test_master_journal_workbook.py::test_checked_in_trading_journal_resync_preserves_symbols_and_trade_log_borders -q --basetemp .pytest_tmp_workbook_final`: 2 passed.
- Real workbook `journal/Trading Journal.xlsx` was updated with two consecutive source-based data-only resyncs.
- Real workbook open/save/reopen inspection confirmed SYMBOLS merges, AT/AU/AV widths and data boundaries, and Trade Log `X1`/`AC1` medium right borders with cleared fills.

Notes and remaining risks:
- Pytest cache warnings remain due local `.pytest_cache` permissions; repo-local `--basetemp` runs completed.
- The focused workbook regression pair is slow in this environment, so the full workbook test module was not run end-to-end.
- Pre-existing runtime/local-state files remain dirty and should stay unstaged: `render/data/*`, `bybit_monitor/custom_alerts.json`, `state_backup.json`, and `state_manifest.json`.

Push workflow:
- `PATCH_DESKTOP_PUSH.bat` must run after this summary and verify local HEAD equals `origin/master` before the task is complete.
