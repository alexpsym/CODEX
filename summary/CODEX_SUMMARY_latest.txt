Instrument lookup, calculator, dashboard, and Trading Journal repair summary

Status:
- Implementation and workbook repair completed locally.
- This summary was written before the mandatory PATCH_DESKTOP_PUSH.bat run.
- Final push verification is recorded in the Codex final response after the script completes.

Instrument Lookup:
- Removed the Crypto/FX toggle and JPG download control from the lookup page.
- Lookup now infers FX versus crypto from the query and keeps history URLs to /instrument-lookup?q=... without an asset parameter.
- Market snapshot hides raw openInterest and shows openInterestValue as USD.

Calculator:
- Reworked the calculator page into a clearer two-column desktop layout with compact table-style sections and a mobile single-column fallback.
- Quote results now render as a stable table.
- Calculator instrument specs and journal summary panels are loaded from the resolved symbol and recover from failures instead of staying in a loading state.
- Journal summary now reads from the single master workbook when configured, with runtime fallback only when needed.
- Crypto journal summary matching now requires the canonical normalized symbol instead of unsafe prefix matching, so BTCUSDT/BTC/USDT/Bitcoin USDT aliases resolve to the same workbook rows without cross-symbol leakage.

Dashboard:
- Local dashboard main buttons no longer expose Open Orders or Pine as standalone main buttons.
- The Pine scripts area now lives permanently under Orders / Positions with copy buttons and a fallback textarea when clipboard writes are unavailable.
- The main dashboard workspace is wrapped so local panels remain visually grouped without moving the Pine fallback out of reach.

Trading Journal workbook generation:
- Stats 1 freeze panes repair to B2 instead of B88.
- P&L Calendar one-line layout clears stale fills for blank/no-trade months and keeps total-trades rows neutral.
- Trade Log data row heights remain normalized during data-only updates and Excel-open polish, even for wrapped long notes.
- Target-R recommendations now enforce a 1.50R floor and avoid displayed recommendations that round to the current median.
- Checked-in journal/Trading Journal.xlsx was refreshed from its own Trade Log source rows and polished with the new repair rules.

Tests and checks:
- tests/test_dashboard_home_layout.py: 18 passed.
- tests/test_master_service_trading_journal_stats.py: 58 passed.
- tests/test_master_journal_workbook.py: 146 passed across chunks.
- tests/test_master_service_calculator_api.py and tests/test_master_service_instrument_specs.py: 93 passed.
- tests/test_open_orders_page.py and tests/test_spread_monitor_dashboard.py: 32 passed.
- tests/test_calculator_js_syntax.py, tests/test_dashboard_js_syntax.py, and tests/test_calculator_js_behavior.py: 44 passed.
- git diff --check passed with CRLF normalization warnings only.

Runtime-state note:
- Existing dirty runtime/local files remain intentionally local and are excluded by PATCH_DESKTOP_PUSH.bat, including bybit_monitor/custom_alerts.json, render/data/*, and state_manifest.json.
- Test-generated changes to render/data/pending_webhooks.json, render/data/webhook_attempts.json, and state_backup.json were restored before summary and push.
