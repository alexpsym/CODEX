Final implementation summary - Pine markers, Oanda commission repair, monthly dedupe, workbook formatting, and import performance

Publication status
- Work was performed in C:\GPT\CODEX-master.
- This summary is written before publication as required by AGENTS.md.
- PATCH_DESKTOP_PUSH.bat must run next. The task is not complete until it succeeds and verifies local HEAD equals origin/master.

1. Pine session and historical FX markers
- Reduced sessionMarkerHalfLengthMultiplier from 0.60 to 0.30 without changing funding or options-expiry line behavior.
- Historical FX start markers now use the exact configured timestamp when a loaded candle spans it and otherwise use the first loaded candle at or after the configured start.
- The marker remains broker-independent, one-time only, and anchored with xloc.bar_time.
- Preserved every pair/date mapping, including EURUSD => 1999-01-04 00:00 GMT.

2. Oanda financing-aware Commission
- Displayed Commission now equals absolute broker commission/GSL fees plus absolute allocated financing, funding, or swap.
- Financing that is already represented in reported Oanda P/L is not subtracted again from Net P/L.
- Opening and closing transaction fees are aggregated, financing is allocated proportionally for multi-leg API transactions, and funding/financing/swap transaction labels are recognized.
- Sanitization, canonical replacement, and persisted-row repair retain financing-derived Commission.
- Added diagnostics for broker commission, financing commission, displayed commission, and whether displayed Commission includes financing.
- The repaired row oanda_export:demo:652:664 now has Commission/Fees 0.9608, Swap -0.9608, and unchanged Net P/L 17.4233 in both render/data/trading_journal.json and state_backup.json.

3. Monthly row dedupe and Trade Log preservation
- Canonical monthly AUD revaluation Row IDs are deduplicated only within the monthly row class; unrelated ordinary trades are not deduplicated.
- Duplicate candidates are ranked so the most complete valid monthly record survives.
- Presentation preservation is now keyed by Row ID, so borders, comments, hyperlinks, row heights, number formats, alignment, protection, and non-colour font properties follow surviving rows when dedupe shifts row numbers.
- Generated Trade Log fills are reapplied after preserved presentation restoration: wins green, losses red, zero-P/L trades unfilled, and cashflow/monthly rows unfilled.
- Conditional formatting is rebuilt through the current final populated Trade Log row.

4. Oanda import performance
- Identical imports verify uploaded projections and account balance against the workbook source already read during preflight.
- The duplicate no-op path skips row upsert, authoritative snapshot generation, derived-sheet rebuild, workbook replacement, full snapshot verification, SQLite persistence, and GitHub workbook sync.
- Changed imports reuse one prebuilt authoritative snapshot through workbook update and verification.
- Journal row loading uses a path/file-stat cache with defensive deep copies.
- Recommendation/statistics work reuses row signatures and scoped results within each calculation instead of repeatedly hashing/recalculating overlapping groups.

5. Live workbook repair and verification
- Repaired journal/Trading Journal.xlsx through the production changed-import path with external Dropbox/GitHub side effects disabled; local JSON, workbook, view-cache, and SQLite persistence remained enabled.
- Trade Log contains 1,547 populated rows and 1,547 unique Row IDs.
- Each of the five monthly IDs for 2026-03 through 2026-07 appears exactly once and has no red/green semantic fill.
- Row oanda_export:demo:652:664 is fully green, Commission is 0.9608, and Net P/L remains 17.4233.
- Conditional formatting contains the two trade-only win/loss formulas over A4:AX1550, the final populated row.
- Comparison against the pre-repair HEAD workbook preserved sheet order, active sheet, all formulas, 1,211 Trade Log hyperlinks, all pre-existing comments, borders, column widths, row heights, merges, freeze panes, chart anchors, and seven defined names. One generated STATS2 risk-of-ruin diagnostic comment was added; no existing comment was removed.
- The repository-native/openpyxl verifier passed. Artifact-tool visual import was not available because an existing chart-axis value in the source workbook exceeds that importer's UInt32 limit; the workbook itself opens and verifies with the repository's native tooling.

6. Measured import timings on the repaired live journal
- Changed one-row Oanda import: 245.015550 seconds wall time.
  - Parse 0.122501s; P/L inference 0.001749s; upsert 2.630175s; authoritative snapshot 31.276777s; workbook sync 196.418293s; row-survivor verification 2.323346s; workbook snapshot verification 5.507340s; SQLite persistence 1.395522s.
- Immediate identical import: 8.759790 seconds wall time.
  - Parse 0.094030s and P/L inference/preflight 0.358427s.
  - Upsert, snapshot build, workbook sync, row verification, workbook snapshot verification, snapshot persistence, and GitHub sync each reported exactly 0.0s.
  - The workbook, journal JSON, view cache, and SQLite size/mtime/hash fingerprints were unchanged by the identical import.

7. Tests and checks
- tests/test_custom_indicator_pine.py: 15 passed.
- tests/test_repair_oanda_journal.py: 3 passed.
- tests/test_master_service_oanda_history_export.py: 46 passed.
- Broader Oanda CRUD/statistics suites: 274 passed.
- Focused monthly dedupe, direct-fill, conditional-formatting, and preservation-mode workbook coverage: 10 passed, 171 deselected.
- python -m py_compile for the changed Python modules and tests: passed.
- git diff --check: passed with only normal LF-to-CRLF working-copy notices.
- Pytest emitted existing FastAPI on_event deprecation warnings and cache/temp permission warnings; all selected tests passed.

8. Safety and repository hygiene
- Preserved pre-existing runtime-state modifications in render/data/monthly_aud_revaluation.json, monthly_aud_revaluation_state.json, pending_webhooks.json, trade_contexts.json, and webhook_attempts.json; PATCH_DESKTOP_PUSH.bat must leave runtime data unstaged and unpublished.
- Added .pytest-*/ to .gitignore so the test runner's workspace-local scratch directories cannot be committed; repository policy prohibited recursive deletion, so those scratch directories were left untouched.
- No secrets, environment files, logs, caches, or unrelated runtime data are intended for the commit.

Mandatory completion step
- Run repository-root PATCH_DESKTOP_PUSH.bat.
- Do not report completion unless it succeeds and proves local HEAD equals origin/master.
