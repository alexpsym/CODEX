Final narrow follow-up summary

Status:
- Removed the unintended local-profile History Export main script button from _profile_main_buttons().
- Preserved the inline History Export dashboard panel, /merged/history, legacy broker-history redirects, exporters, and history APIs.
- Corrected the stale calculator API test to the inline-History contract: Calculator and Scanner remain merged main rows, while History must not appear as a separate scripts-rail row.
- Added a no-ResizeObserver window resize fallback for the Instrument Lookup Recent Trades top scrollbar.
- Did not rewrite or resave journal/Trading Journal.xlsx.
- Did not intentionally edit state_backup.json. It was already dirty local runtime state at the start of this final brief and remains excluded from the intended commit.

Files changed intentionally:
- render/master_service.py
- render/static/instrument_lookup.js
- tests/test_dashboard_home_layout.py
- tests/test_master_service_calculator_api.py
- summary/CODEX_SUMMARY_latest.txt

Implementation notes:
- The duplicate main button entry {"id": "history", "name": "history", "label": "History Export", "open_url": "/merged/history", "dashboard_main_view": True} was removed.
- Instrument Lookup now binds a single guarded window resize listener only when ResizeObserver is unavailable and window.addEventListener exists. The fallback uses the existing scheduled/requestAnimationFrame update path and preserves the existing ResizeObserver path and bidirectional scrollLeft sync.
- Added a JS behavior test that runs without ResizeObserver, captures the window resize handler, changes simulated table/viewport width, invokes resize, waits for the scheduled update, verifies the top spacer width changes, and confirms top-to-table and table-to-top scrolling still synchronize.
- Confirmed the source no longer contradicts the local-profile tests: no "name": "history" main button remains, tests/test_master_service_calculator_api.py now has merged_names = {"calculator", "monitor"}, and the dashboard/spread tests still assert "history" not in by_name.

Verification performed:
- python -m py_compile render/master_service.py tools/master_journal_workbook.py: passed.
- node --check render/static/calculator.js: passed.
- node --check render/static/instrument_lookup.js: passed.
- Focused no-ResizeObserver scrollbar and calculator API contract tests: 2 passed, 4 warnings.
- Requested python -m pytest -q -p no:cacheprovider tests/test_dashboard_home_layout.py: 18 passed before 6 setup errors caused by PermissionError scanning C:\Users\User\AppData\Local\Temp\pytest-of-User.
- Rerun with repo-local temp, python -m pytest -q -p no:cacheprovider --basetemp=.pytest_tmp_final_narrow_dashboard tests/test_dashboard_home_layout.py: 24 passed, 41 warnings.
- python -m pytest -q -p no:cacheprovider --basetemp=.pytest_tmp_final_narrow_calc_api tests/test_master_service_calculator_api.py: 95 passed, 4 warnings.
- python -m pytest -q -p no:cacheprovider --basetemp=.pytest_tmp_final_narrow_spread tests/test_spread_monitor_dashboard.py: 13 passed, 8 warnings.
- python -m pytest -q -p no:cacheprovider --basetemp=.pytest_tmp_final_narrow_calc_js tests/test_calculator_js_behavior.py: 25 passed.

Workbook and runtime/state scope:
- git diff --name-only -- journal/Trading Journal.xlsx returned no output; the workbook was untouched by this correction.
- state_backup.json remains dirty local runtime state from before this final brief and is excluded by PATCH_DESKTOP_PUSH.bat along with other runtime/data paths.
- No runtime timestamps, caches, credentials, workbook edits, or local state files are intended for this follow-up commit.

Push workflow:
- PATCH_DESKTOP_PUSH.bat must run after this summary and verify local HEAD equals origin/master before the task is complete.
