OANDA volatility major-pair sections and Local Trading Tools Ctrl+Q shutdown

- Oanda Volatility now separates its existing returned currency rows into independently ranked Major Forex Pairs and Other Forex Pairs sections, using the seven requested OANDA major instruments. It still uses one cached/fetched dataset and retains all existing ATR, refresh, status, formatting, and route behavior.
- Local Trading Tools now prints a Ctrl+Q instruction and, only for an interactive Windows local-master console, runs one low-idle terminal-local watcher. Ctrl+Q reuses the same marker, logging, and delayed owned-process exit helper as `/api/local-exit`; plain Q does nothing and Ctrl+C handling is unchanged.
- No launcher, browser-tab, generic process termination, external network, or unrelated-tool changes were made.
- Targeted verification passed once: `tests/test_oanda_volatility.py::test_currency_rows_split_majors_first_without_duplicates`, `tests/test_master_service_local_exit.py::test_terminal_ctrl_q_uses_canonical_local_exit_but_plain_q_does_not`, and `tests/test_master_service_local_exit.py::test_local_exit_success` (`3 passed`).
- Changed only `render/oanda_volatility.py`, `render/master_service.py`, `tests/test_oanda_volatility.py`, `tests/test_master_service_local_exit.py`, and this summary.
