Completed the follow-up Local Trading Tools launch crash visibility fix.

Changes made:
- Changed run_local_master_control.bat so the visible worker window no longer launches through the disappearing `cmd /c` path.
- Added a `__worker_console` wrapper launched with `cmd /k`; it redirects the real worker to logs\LocalTradingTools-worker-latest.log, then leaves the console open on abnormal worker exit.
- The worker wrapper now prints the worker log path, prints the last 40 worker log lines on abnormal exit, and waits for a keypress before closing so startup crashes stay readable.
- Added LOCAL_MASTER_NORMAL_EXIT_FILE so normal user-triggered app Exit can still close the worker console automatically instead of being held open by the new failure pause.
- Kept persistent logs for logs\LocalTradingTools-launch-latest.log and logs\LocalTradingTools-worker-latest.log; logs remain ignored by Git.
- Updated TradingToolsLauncher.cs so non-zero launcher failures always include the exact worker log path in the MessageBox, with a useful worker log tail or an explicit no-output placeholder.
- Updated launcher and merged-monitor tests so they no longer protect the old disappearing `cmd /c` worker command and instead assert the persistent-console abnormal-failure branch.

Verification run:
- python -m pytest tests\test_windows_launchers.py -q
  Result: 34 passed.
- python -m pytest tests\test_merged_monitor_status_env.py -q
  Result: failed only because this environment cannot scan C:\Users\User\AppData\Local\Temp\pytest-of-User for pytest temp dirs (PermissionError [WinError 5]).
- python -m pytest tests\test_merged_monitor_status_env.py -q --basetemp .pytest_tmp_merged_monitor
  Result: 42 passed, 4 warnings.
- build_windows_launchers.bat
  Result: Successfully created C:\Users\User\Documents\GPT\CODEX-master\Local Trading Tools.exe with TT.ico resolved.

Notes:
- No Trading Journal workbook or layout files were changed.
- Local ignored/generated files remain uncommitted, including Local Trading Tools.exe, logs, .pytest_tmp_merged_monitor, caches, render/data runtime state, and state_backup.json.
- An unrelated modified file under custom instructions/ remains untouched and is intentionally kept out of this launcher fix commit.
- This summary file was written before running PATCH_DESKTOP_PUSH.bat as requested; the final Codex response reports the push result from PATCH_DESKTOP_PUSH-latest.log.
