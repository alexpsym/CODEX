Instrument Lookup Binance specifications and interval-range correctness

- Root cause 1: the canonical Instrument Lookup crypto path resolved and loaded specifications only through Bybit. ZECUSDT is an active Binance USDⓈ-M perpetual but was not resolved by that source, so the independent journal request succeeded while specifications returned not found.
- Root cause 2: movement ranges were also sourced through Bybit interval aliases instead of the required Binance USDⓈ-M contract. The frontend did not reuse or mis-index values; equal live values can legitimately occur when current candles overlap. The corrected path now associates every response key with its exact Binance interval and a host+symbol+interval cache key.
- Added Binance USDⓈ-M exchange-info lookup with successful-response caching, stale-cache retry behavior, no negative caching, precise upstream failure reporting, and generic normalization for compact, slash-separated, and space-separated symbols.
- Parsed exchange filters by filterType, independent of array order, and retained available specifications when optional filters are absent.
- Preserved the existing (high - low) / open formula, one-candle lookback, response field names, journal independence, frontend rendering/formatting, route, and UI layout.
- Changed only render/master_service.py, tests/test_master_service_instrument_specs.py, and this summary. render/static/instrument_lookup.js did not require a change.
- Targeted verification: the two exact new pytest nodes passed together; after the route-level error translation changed, only the affected ZEC node was rerun and passed. No complete test file, suite, broad lint, syntax-only duplicate check, or general smoke test was run.
- Final requirement comparison: ZECUSDT-shaped reordered/optional-filter metadata is parsed generically; the five requested BTC labels use deliberately distinct interval fixtures and distinct cache keys; journal/UI behavior is unchanged; all task files are authorized.
- Live evidence: one focused Binance exchange-info read confirmed ZECUSDT is TRADING/PERPETUAL and lacks several optional filters; one focused BTC kline read confirmed exact interval requests and demonstrated that equal current-candle values can be legitimate.
