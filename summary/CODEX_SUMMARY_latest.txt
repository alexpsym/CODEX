FT6 stale provisional candidate/fresh-seed handoff correction

- Saved the exact brief at `CODEX_BRIEF_FIX_STALE_PROVISIONAL_BLOCKS_FRESH_SEED.txt`. Confirmed the required starting commit `3fe106e6e6593572966ad7484c94261a79755285`; the initial worktree contained only the same unrelated user-owned deletions and runtime/data changes, all preserved and left unstaged.
- Exact newer-origin seed-loss cause: direct current normal-seed acceptance required the same-direction candidate to be inactive. A stale active candidate absorbed the current seed first, then `TryPromoteCandidate()` rejected the combined candidate because the confirmed `originBar` was newer than the old `firstBar`, leaving the valid current seed lost inside an incompatible object.
- Exact expiry seed-loss cause: expiry was checked only inside `TryPromoteCandidate()`, after the current seed had already been appended. Resetting the expired candidate therefore deleted the current valid seed on the same bar.
- Added symmetric pre-seed retirement. Before a qualifying current candle may mutate an active same-direction candidate, the strategy checks the recovery window and latest confirmed directional origin. It retires and resets the old object when the window expired or `originBar > firstBar`, logging the old first bar and current/new-origin bar.
- After retirement, the same current candle is processed exactly once through `CanSeedImpulse()`. A valid normal seed starts the fresh parent immediately (including while a position is busy, without opening an order). A real origin/post-consumption gate still applies and is logged; retirement does not bypass consumed-parent restrictions.
- A compatible active candidate is not discarded. The current normal seed remains cumulative evidence for that earlier candidate, which promotes through the existing chronological replay and passed-resumption protections. If compatible replay consumes the old opportunity, the current candle is not double-processed without a distinct newer confirmed origin; the conservative disposition is logged.
- A current oversized candle after retirement starts a clean provisional candidate at the current bar. `normalSeedSeen`, `continuationSeen`, `firstExtreme`, `firstBar` and other evidence are reset; it cannot start a normal parent and still needs a later normal seed or later same-direction endpoint advancement. The 1.5 ATR maximum-body meaning is unchanged.
- Preserved chronological replay, historical endpoint/PB1/qualification state, passed/current-bar resumption consumption, all confirmation modes, time-valid minor swings, oversized opposing/doji/non-advance rejection, parent dominance, independent-leg gates, passive detection, no-pyramiding, busy consumption, trade-direction controls and bullish/bearish symmetry. No user-visible setting or export changed; the guide, `.def`, TradingView, MT5 and test files were not modified.
- One narrowly scoped deterministic handoff verification passed all seven requested groups in bullish and bearish directions: newer-origin retirement plus fresh normal acceptance; expiry-bar retirement plus fresh normal acceptance; compatible earlier-candidate promotion/replay; clean oversized rotation without inherited evidence or normal acceptance; old/new post-consumption origin eligibility; symmetry; and exactly-once seed/parent processing with no order while busy.
- The previous complete 18-case replay harness, complete test files, full suites, broad checks, smoke tests, historical backtests, optimisations and simulations were not run.
- One actual affected native 32-bit FT6 compile passed using the canonical `.cpp`/`.def`, installed `C:\ForexTester6\Examples\Strategies\C++` API headers and `oleaut32.lib`.
- Fresh build: `C:\GPT\CODEX-master\.codex_tmp_ft6_candidate_handoff_build\RangeBreakoutPullbackStrategy.dll`, 147968 bytes, modified `2026-09-01T22:50:36.4868362+10:00`, SHA-256 `D7F9E50593934A34A068C8F657BE6DFC45AB58F6595FDF32B7F4966956049CE9`.
- Previous deployment: `C:\ForexTester6\Strategies\RangeBreakoutPullbackStrategy.dll`, 146944 bytes, modified `2026-09-01T22:34:27.7050053+10:00`, SHA-256 `B4F6EF42D10050D4ADA05EEB29DF0BBD6AC64A1CCC284A12794C88994BA156AF`.
- Forex Tester 6 was confirmed closed before copy. Final deployment: `C:\ForexTester6\Strategies\RangeBreakoutPullbackStrategy.dll`, 147968 bytes, modified `2026-09-01T22:50:36.4868362+10:00`, SHA-256 `D7F9E50593934A34A068C8F657BE6DFC45AB58F6595FDF32B7F4966956049CE9`. Built and deployed hashes match exactly.
- No implementation limitation or required user action remains.
