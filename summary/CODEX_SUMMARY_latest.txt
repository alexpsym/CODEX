Final trendline-panel guidance correction

- Required/verified starting HEAD: 1ef3808b8ee8b58f04fa25903ec847c090da669c. Preserved all pre-existing modified, deleted and untracked user-owned paths. The real trendline registry and lock file were not accessed.
- Replaced obsolete stored-plan-only guidance in the actual calculator panel. It now accurately states that armed Bybit/OANDA plans are monitored only after Start monitoring, Local Trading Tools must remain running with the computer awake, test plans simulate, fresh validated eligible non-test plans can submit once, live plans need explicit confirmation, TradingView anchors are manual, and Pepperstone remains on its MT5 trendline EA. IDs, controls, layout, styles, monitor Start/Stop behavior and execution/API logic were unchanged.
- Exact focused invocation: python -m pytest -q --tb=short tests/test_master_service_calculator_api.py::test_local_trendline_panel_describes_current_execution_behavior
  Result: passed. The test renders the actual calculator response, asserts each accurate guidance statement, both obsolete phrases absent, and existing Start/Stop controls retained. No other tests were run or rerun.
- Changed only: render/master_service.py; tests/test_master_service_calculator_api.py; summary/CODEX_SUMMARY_latest.txt. Scoped git diff --check passed.
- No real/live/demo service, monitor, executor, broker, quote/account/order, exchange, Render, TradingView, browser, webhook, notification or production-data operation ran. No implementation blocker remains.

Phase 11 corrective follow-up: monotonic terminal timestamps and durable reconciliation visibility

- Required/verified starting HEAD: 1b62ac77a92de29a67657a80b80dd4daa19d0aba. All pre-existing unrelated modified/deleted/untracked paths were preserved. The real trendline registry and sibling lock file were never accessed.
- Broker success, definite rejection, ambiguous failure and missing-reference resolution now read the injected clock after the adapter completes/raises, rather than reusing claim time. Cancellation continues to preserve CancelledError; simulations remain broker-free. All durable store transitions normalize supplied timestamps against prior durable markers under the registry lock, so terminal recorded_at_ms, updated_at_ms and lifecycle time are equal and cannot precede submission or earlier updates, even after a backwards clock change.
- Reconciliation now counts exactly claimed, submitting and uncertain from current durable state on every status request, including newly reconstructed/stopped executors. Completed cycles read the post-cycle count. Registry/lock failure returns JSON null (unknown), never a false zero, with an allowlisted warning that automatic retry remains disabled. The existing known-state warning wording was retained during review for compatibility. No raw exception or broker data is exposed.
- The existing calculator monitor renderer displays singular/plural counts and manual-reconciliation warnings while stopped/running, plus the safe unknown-state warning. Start/Stop behavior and controls remain unchanged; no retry/rearm/reconciliation controls were added.
- Exact pytest invocation: python -m pytest -q --tb=short tests/test_trendline_executor.py::test_phase11_terminal_timestamps_and_durable_reconciliation_status tests/test_calculator_js_behavior.py::test_trendline_reconciliation_warning_is_visible
  Result: 2 passed, 2 pytest-cache permission warnings in 3.01s. One invocation only; no second invocation, old passing node, suite, smoke or expanded test ran.
- Python coverage uses temporary registries and gated mocked submissions: advancing and backwards clocks, matching durable timestamps, accepted/rejected/uncertain/missing-reference/cancelled outcomes, safe simulation, post-cycle and stopped reconstructed counts, read/lock unknown-state handling, failed pre-submission persistence with zero adapter calls, and no retries. The VM executes the production renderer unchanged in isolation, checks stopped/running and zero/singular/plural/unknown rendering and button states, and makes zero requests. Full browser/service integration was intentionally not run.
- Changed paths: render/trendline_plans.py; render/trendline_executor.py; render/static/calculator.js; tests/test_trendline_executor.py; tests/test_calculator_js_behavior.py; summary/CODEX_SUMMARY_latest.txt. Complete scoped diff reviewed; git diff --check passed.
- No live/demo executor, monitor, broker, quote/account/order, exchange, Render, TradingView, browser, webhook, notification or production-data operation ran. Only authorized Git delivery/checks use the remote. No remaining implementation blocker. Interrupted/uncertain submissions still require manual broker reconciliation and are never automatically retried.

Phase 11: restart, reconnect, gap and duplicate-execution safety

- Baseline: de7aca37a4ea5df84dc1d84a679c8b23aee72b28. Preserved every pre-existing unrelated dirty-worktree file. The real runtime trendline registry was not read, created, migrated or modified.
- Added atomic begin_submission: only a matching non-test claimed plan can become submitting, and persistence must succeed before the adapter is invoked. Outcomes resolve from submitting; test simulations can resolve directly from claimed. Execution keys remain unchanged.
- Interrupted claimed/submitting plans are never automatically rearmed, retried or assigned an invented result. Reconstructed executors skip all claimed/submitting/terminal records and report unresolved counts with a manual broker reconciliation message. In-process cancellation during submission attempts a safe uncertain result and re-raises CancelledError; persistence failure leaves durable submitting intact and non-retryable.
- Registry operations now use a bounded thread lock plus a stable sibling OS lock file (Windows msvcrt; POSIX flock). Default acquisition budget is two seconds, capped at five. Atomic JSON replacement remains unchanged; lock files are not unlinked, and the OS releases locks on process exit. Concurrent cycles are serialized per executor; competing instances/processes still arbitrate the single durable claim.
- Start/stop and worker termination clear observations. Candidate/structural/final-calculation failures clear the affected crossing history; registry-wide failure/cancellation clears all history. Reversed/equal quote times fail closed. Existing freshness/gap checks reseed after long gaps; reconnect/restart requires fresh ordered samples for crossing/gap-through, while one fresh inside-band Touch remains eligible. Expiry remains checked before quotes, after calculation and immediately before claim.
- Exact focused command (wrapped in subprocess.run with timeout=120): python -m pytest -q --tb=short tests/test_trendline_plans.py::test_submission_transitions_restart_and_cross_process_claim_lock tests/test_trendline_executor.py::test_executor_restart_reconnect_submission_cancellation_and_duplicates
  Result: 2 passed, 2 pytest-cache permission warnings in 8.28s. One invocation only; no corrective rerun or Phase 10 node rerun.
- Tests use temporary registries, spawned lock/claim-only processes, injected quotes/calculations/submissions and a stub monitor loop. They cover durable transitions/reloads, write failure, Windows cross-process timeout and competing claims, fresh gap prices, restart/reconnect/time gaps, invalid candidate/final samples, expiry boundaries, failed begin_submission with zero adapter calls, cancellation with/without outcome persistence, concurrent instances and inert unresolved/terminal reconstruction. The process-kill window is modeled by reloading persisted claimed/submitting records, not by killing a real worker. One older test expectation was aligned to submitting without rerunning that node.
- Changed only: render/trendline_plans.py; render/trendline_executor.py; tests/test_trendline_plans.py; tests/test_trendline_executor.py; summary/CODEX_SUMMARY_latest.txt. Complete task diff reviewed against Phase 11; scoped git diff --check passed.
- No real/live/demo monitor or executor, broker, exchange, quote service, account, order, Render, TradingView, webhook, browser, notification or production-data operation ran. Monitoring remains stopped. Git remote checks/delivery only. No known implementation blocker; unresolved interrupted submissions always require manual broker reconciliation and are never automatically retried. POSIX locking is implemented but this focused verification ran on Windows.

Phase 10 corrective continuation: private snapshots, broker outcomes and monitor controls

- Baseline: 5d215f4b33a57cc15fb3946f160db1aea92d96bb. Preserved all pre-existing unrelated worktree changes; no runtime state was created or edited by this task.
- Moved trusted-snapshot selection to a private Python-only calculator keyword. Public body flags cannot enable it. The real Bybit/OANDA calculator supplies its actual sizing bid/ask, returned instrument, broker timestamp and tick size; candidate and final snapshots share strict structural, instrument, market-status and freshness checks. Candidate polling stays lightweight, with one candidate and one calculator pricing acquisition before submission, and no post-calculation trigger quote fetch.
- Final trigger checks preserve the pre-candidate observation, reread lifecycle/authorization, and atomically check revision/execution identity at claim. Live authorization must match the exact armed revision; historical authorization remains loadable after claim/terminal transitions. Cancellation/expiry still clear authorization and legacy unauthorized live plans cannot claim.
- Submission adapters return only affirmative, validated broker order references using the existing OANDA create/fill extraction and Bybit order ID. Private request-state tracking distinguishes known pre-POST/negative acknowledgements from uncertain post-request transport, malformed-result and protection failures. Generic ValueError no longer means definite rejection. Claimed/uncertain plans are not retried.
- Per-plan and outer-cycle errors now use allowlisted messages only; clean cycles clear previous errors. UI monitoring failures recover authoritative status once and restore Start/Stop controls. Cancelled live confirmation sends nothing; acceptance sends exactly the Boolean confirmation body. Limit draft editing restores the order intent and persisted entry, clearing stale market entry values.
- Initial focused invocation (120-second subprocess cap): python -m pytest -q tests/test_master_service_calculator_api.py::test_trendline_real_calculator_snapshots_and_broker_outcomes tests/test_trendline_executor.py::test_executor_final_snapshot_outcomes_and_authorization tests/test_calculator_js_behavior.py::test_trendline_monitor_ui_failures_live_arm_and_limit_round_trip
  Result: both Python nodes passed; the new UI VM fixture failed because it did not provide AbortController for canonical symbol resolution. Supplied the browser capability in the fixture and asserted resolution completes before Save; no production shortcut or skipped interaction was introduced.
- Corrective invocation, failed UI node only (120-second subprocess cap): python -m pytest -q --tb=short tests/test_calculator_js_behavior.py::test_trendline_monitor_ui_failures_live_arm_and_limit_round_trip
  Result: 1 passed, 2 pytest-cache permission warnings in 0.55s. Passing Python nodes and older tests were not rerun. Total: three new focused nodes passed in two pytest invocations.
- Coverage: real calculator/placement adapters with low-level mocked Bybit/OANDA data and order I/O, market/limit payloads, two pricing acquisitions, public snapshot-flag rejection, OANDA instrument/tradeability checks, accepted order references and definite/ambiguous outcomes; injected executor touch/cross/gap, moved-back/cancel/expiry rejection, invalid final quotes, authorization lifecycle, isolation/error clearing and no duplicate submission; executable VM live-arm/limit round-trip and Start/Stop success/failure controls.
- Changed: render/master_service.py; render/trendline_executor.py; render/trendline_plans.py; render/static/calculator.js; tests/test_master_service_calculator_api.py; tests/test_trendline_executor.py; tests/test_calculator_js_behavior.py; summary/CODEX_SUMMARY_latest.txt. Task-scoped git diff --check passed.
- Monitoring remained stopped. No real executor worker, broker, exchange, account, quote-service, order, Render, TradingView, webhook, browser, notification or production-data operation ran; only mocked operations and authorized Git remote checks/delivery. No known blocker for this correction; Phase 11 remains unstarted and live reliance still requires that separate safety phase.

Phase 10 corrective continuation: final calculator snapshot and live arm authority

- The private in-process calculator path now returns the exact canonical broker snapshot it used for sizing (instrument, bid, ask, broker timestamp, and tick size). The executor uses that returned snapshot for final trigger revalidation and no longer fetches a third post-calculation quote.
- Added durable `limit_entry_price` handling: newly saved limit plans require it, it round-trips through calculator plan data, market plans clear it, and a legacy limit plan without it fails closed before submission.
- Live authorization is now bound to the server-created arm instance rather than invalidated by normal claim revisions. Cancellation/expiry clear it, draft edits clear it, expired live arming is rejected, and the arm route accepts only literal JSON boolean `confirm_live_execution: true` before atomically creating the credential-free authorization.
- PASS prior focused invocation (not rerun): `python -m pytest -q tests/test_trendline_executor.py::test_trendline_executor_fresh_claim_recalculation_and_one_submission tests/test_trendline_executor.py::test_trendline_executor_rejects_stale_and_simulates_test_without_submission tests/test_master_service_calculator_api.py::test_local_trendline_executor_start_stop_status_is_profile_gated tests/test_calculator_js_behavior.py::test_local_trendline_plan_ui_saves_manual_anchors_and_manages_status` — `5 passed in 11.42s`.
- PASS final permitted consolidated node: `python -m pytest -q tests/test_master_service_calculator_api.py::test_trendline_final_calculator_snapshot_and_literal_live_arm` — `1 passed in 11.75s` (framework/pytest-cache warnings only). It verifies one calculator snapshot with no third quote, exact final snapshot propagation, and strict server-side live-arm Boolean/authorization behavior without broker activity.
- No executor worker, broker, exchange, account, quote service, order, Render, TradingView, webhook, browser, notification, or production-data operation ran. Monitoring remains stopped; Phase 11 has not been started.

Phase 10 corrective follow-up: real executor integration

- Replaced the polling adapter's calculator-derived pseudo quote with lightweight canonical Bybit/OANDA snapshots containing the broker instrument, bid, ask, broker timestamp, and tick size. Missing timestamps, malformed/wrong/crossed quotes, and absent precision now fail closed rather than substituting entry price or manufacturing time.
- Candidate detection still uses the lightweight snapshot, but final sizing obtains a fresh canonical calculator result and a final quote is re-evaluated before the plan is reread and atomically claimed. Revision, execution-key, cancellation, expiry, and live-authorization changes therefore prevent a stale candidate from submitting.
- Added revision- and execution-key-bound, credential-free durable live authorization. Local live non-test arming requires explicit confirmation data; older armed live plans without it remain armed and unclaimed. Test plans simulate, demo plans remain eligible, and plan updates clear authorization.
- Corrected per-plan cycle isolation, sanitized terminal failure details, broker order-ID extraction, truthful monitor/execution response flags, and monitor UI state restoration. The UI stores the most recent monitor response, clears pending state before rendering success/failure controls, and sends the explicit live authorization payload.
- Initial focused invocation: `python -m pytest -q tests/test_trendline_executor.py::test_trendline_executor_fresh_claim_recalculation_and_one_submission tests/test_trendline_executor.py::test_trendline_executor_rejects_stale_and_simulates_test_without_submission tests/test_master_service_calculator_api.py::test_local_trendline_executor_start_stop_status_is_profile_gated tests/test_calculator_js_behavior.py::test_local_trendline_plan_ui_saves_manual_anchors_and_manages_status` — 3 passed; the two parameterized fresh-claim cases exposed only a renamed local calculation variable used at submit.
- PASS corrective rerun of failed node only: `python -m pytest -q tests/test_trendline_executor.py::test_trendline_executor_fresh_claim_recalculation_and_one_submission` — `2 passed in 0.32s` (pytest-cache permission warnings only).
- No executor worker, broker, exchange, account, quote, order, Render, TradingView, webhook, browser, notification, or production-data operation ran. Monitoring remains stopped; Phase 11 has not been started.

Phase 10: local Bybit/OANDA trendline-plan executor

- Added `render/trendline_executor.py`, an injected async executor with duplicate-worker-safe cancellation-event start/stop handling. Each cycle examines only armed/unclaimed plans, rejects bad or stale quotes through Phase 8 evaluation, preserves sample history for cross detection, atomically claims a valid trigger before any calculation/submission action, and persists submitted, simulated, failed, or uncertain terminal outcomes without automatic retries. The local service adapters rebuild calculator inputs in process from the durable plan and call the existing broker placement functions only after the claim.
- Extended the canonical store with an immutable, credential-free terminal execution result and atomic claimed-plan resolution. Test plans record a simulated terminal trigger and never call the injected submit function; an unavailable live-confirmation gate fails closed.
- Added local-only monitor start/stop/status endpoints and minimal calculator controls. Monitoring is stopped after process start, arming alone does not start it, and duplicate Start requests do not create a second worker. No executor was started for verification.
- Initial focused run: `python -m pytest -q tests/test_trendline_executor.py::test_trendline_executor_fresh_claim_recalculation_and_one_submission tests/test_trendline_executor.py::test_trendline_executor_rejects_stale_and_simulates_test_without_submission tests/test_master_service_calculator_api.py::test_local_trendline_executor_start_stop_status_is_profile_gated` — executor nodes passed; the lifecycle node exposed only a misplaced pre-existing Pepperstone assertion in the new test.
- PASS corrective rerun of the failed node only: `python -m pytest -q tests/test_master_service_calculator_api.py::test_local_trendline_executor_start_stop_status_is_profile_gated` — `1 passed in 8.30s` (framework/pytest-cache warnings only).
- Changed only `render/trendline_executor.py`, `render/trendline_plans.py`, `render/master_service.py`, `render/static/calculator.js`, `tests/test_trendline_executor.py`, `tests/test_master_service_calculator_api.py`, and this summary. No executor worker, broker, exchange, account, quote, order, Render, TradingView, webhook, browser, notification, or production-data operation ran. Phase 11 safety/restart/reconnect testing has not been started.

Phase 9: local trendline-plan API and calculator UI

- Added local-only CRUD/status endpoints backed by the existing Phase 8 `TrendlinePlanStore`. They independently reject non-local profiles before state access, return explicit inactive monitoring/execution flags, map resolved calculator fields into the canonical schema, and do not perform broker, quote, webhook, monitor, or execution work.
- Added the existing calculator's hidden-unless-local “Local trendline plans” panel. It accepts manually copied browser-local anchors with UTC previews, uses current resolved calculator configuration without persisting quote/TP/quantity values, supports draft editing, one-confirmation live non-test arming, cancellation, and refresh, and clearly states that monitoring/execution and TradingView drawing import are not implemented.
- The Node/VM action-button failure came from an incomplete fake DOM: rendered `innerHTML` action controls were not modeled/discovered with their `data-tl-*` attributes. The fixture now parses and returns those generated controls via `querySelectorAll`, assigns datasets, implements click listeners, and asserts the expected action exists before exercising edit/arm/cancel. It also preserves the preceding stale-list/debounced-resolution correction: mutation and list-loading state are distinct, and list request sequencing prevents an older bootstrap response from replacing a newly saved plan.
- PASS earlier (not rerun during the final UI correction): `python -m pytest -q tests/test_master_service_calculator_api.py::test_local_trendline_plan_crud_is_profile_gated_and_preserves_calculator_fields`.
- PASS `python -m pytest -q tests/test_calculator_js_behavior.py::test_local_trendline_plan_ui_saves_manual_anchors_and_manages_status` — `1 passed in 0.39s` (two pytest-cache permission warnings). The executable VM test covers local visibility, UTC anchor preview, resolved instrument/current calculator field POST, Touch direction enforcement, duplicate-action prevention, draft edit, live confirmation-gated arm, cancel, refreshes, and no calculator submit/webhook/quote request.
- Changed only `render/master_service.py`, `render/static/calculator.js`, `tests/test_master_service_calculator_api.py`, `tests/test_calculator_js_behavior.py`, and this summary. No broker, exchange, Render, TradingView, browser, webhook, notification, or production-data operation ran. Phase 10 has not been started.

Phase 8: local Bybit/OANDA trendline-plan foundation

- Added `render/trendline_plans.py`: a standalone versioned local registry at `render/data/trendline_plans.json` with strict schema validation, Decimal-string persistence, process-locked atomic read-modify-write saves, and no broker/API/scheduler/Dropbox/Render dependency.
- Plans persist immutable IDs and execution keys, revisions and UTC timestamps, broker/order/risk metadata, two validated anchors, trigger/tolerance/quote freshness limits, expiry, durable status/lifecycle, and an optional durable trigger claim. Explicit create, update, arm, cancel, expire, and atomic duplicate-safe claim transitions reject illegal changes and never rearm persisted claimed/uncertain lifecycle states.
- Pure quote evaluation projects the unrounded Decimal line, selects executable/bid/ask/midpoint price, respects extension and tolerance boundaries, handles touch/gap-through and filtered confirmed crossings, and returns explicit reseed/ineligibility reasons for stale, future, out-of-order, or excessive-gap observations. It cannot submit an order.
- PASS `python -m pytest -q tests/test_trendline_plans.py::test_trendline_plan_schema_projection_evaluation_and_atomic_claims` — `1 passed in 0.52s` (two pytest-cache permission warnings). The test uses only `tmp_path` and covers schema/round-trip, Decimal projection, price bases, trigger semantics, quote validity, lifecycle/claim persistence, and corrupted-state preservation.
- Changed only `render/trendline_plans.py`, `tests/test_trendline_plans.py`, and this summary. No broker, exchange, Render, TradingView, browser, notification, or production-data operation ran.

Phase 7 corrective follow-up: complete OANDA fresh-submit capacity and adjustment reporting

- The calculator-metadata RR revalidation now applies the quote path's zero/unlimited conventions for `maximumPositionSize`, instrument/account margin-rate selection, position-value conversion, estimated initial margin, and `marginAvailable`. Both capacity checks fail before the single OANDA broker POST.
- Revalidation retains Decimal-normalized quoted quantity/entry/stop/TP values and returns the original and final bracket/quantity, minimum reward, effective Net-R, and a Decimal-based TP-adjustment flag as top-level `submit_level_adjustments` from `/api/calculator/submit`. Legacy/manual OANDA payloads remain unchanged.
- PASS (after one bounded endpoint-field correction) `python -m pytest -q tests/test_master_service_calculator_api.py::test_oanda_rr_floor_survives_costs_conversion_sizing_and_submit_revalidation` — `1 passed in 8.90s`. The mocked node verifies exactly one successful POST with fresh units/stop/TP, returned adjustments and Net-R floor, and no POST for maximum-position or refreshed-margin failures.
- Changed only `render/master_service.py`, `tests/test_master_service_calculator_api.py`, and this summary. No live/demo broker, exchange, Render, notification, or production-data request ran.

Phase 7: enforce nominated minimum Net-R targets after costs and final sizing

Defect and invariant implemented

- Pepperstone automatic Net-R targets previously used only nominal rounded risk, allowing a legal undersized lot or retained slippage-buffer risk to produce less than the nominated target. The shared live and Backtest calculations now retain buffered worst-case all-in risk after volume adjustment and use `max(RiskAUD_Target, final rounded all-in risk, final buffered all-in risk)` as the Net-R basis. Thus AUD 10 at 2R requires at least AUD 20 net when final risk is lower, and at least AUD 22 when retained buffered risk is AUD 11.
- Both MQL5 AutoTP helpers validate profit after final TP normalisation with OrderCalcProfit, subtract configured round-trip commission once, move an undershooting target outward by bounded legal point increments, and fail if the floor cannot be established. Buffered risk above RiskAUD_Max is a hard failure before any order is sent. The corrected shared helper is used by standard market, standard limit/trendline, EMA, and Backtest paths; manual TP remains unchanged.
- OANDA calculator RR quoting now determines legal units before calculating the target floor, uses loss-side conversion for all-in loss and gain-side conversion for reward, rounds TP outward, checks the 120% cap, and returns internally consistent estimated loss/reward and Net-R fields. Calculator-metadata OANDA submissions perform one fresh quote/factor/unit/bracket validation before their sole broker request; legacy/manual payloads are unchanged.

Focused verification and deployment

- PASS `python -m pytest -q tests/test_master_service_calculator_api.py::test_oanda_rr_floor_survives_costs_conversion_sizing_and_submit_revalidation` (1 passed in 6.92s after the initial combined run identified and corrected only a test return-key assertion). The prior OANDA test verifies fixed/percentage AUD-10-equivalent floor handling, lower gain-side conversion, whole-unit sizing, outward target precision, 120% failure, one revalidated mocked submit, and one mocked OANDA request.
- PASS `python -m pytest -q tests/test_python_mt5_conversion_files.py::test_mql5_trader_standard_market_is_live_quote_anchored_and_terminal_token_gated` (1 passed in 0.28s). It verifies retained buffered-risk handoff to both live/backtest Net-R helpers, hard risk caps, final TP validation/outward adjustment, and existing live-quote/token protections.
- MetaEditor compilation cycle: Trader.mq5 `0 errors, 0 warnings` (3357 ms); Backtest.mq5 `0 errors, 0 warnings` (1140 ms). MetaEditor returned process code 1 despite both authoritative logs reporting zero errors/warnings.
- Deployed only Trader.mq5 and Trader.ex5 to the exact Pepperstone terminal Experts directory. SHA-256 source: `328100561F5BB198E0323E9D5F5BDB27103FE9C3A3E13F6CF29AF806F97A075D`; compiled/deployed EX5: `7D3377AE49E021DF9E07CFA6347D50C685FB03A769EB008523BEED26DCA9CE25`. Existing deployed files were preserved at `C:\Users\User\AppData\Local\Temp\codex-phase7-trader-backup-20260907-225155`.
- No live/demo trade, broker, exchange, notification, Render, TradingView, or production-data request ran. Phase 7 is complete; modeled costs/quotes do not mathematically guarantee eventual realised P/L against future gaps, slippage, conversion changes, swaps, or unconfigured broker charges.

Exact files changed

- render/master_service.py
- mt5-clone/MQL5/Experts/Trader.mq5
- mt5-clone/MQL5/Experts/Backtest.mq5
- tests/test_master_service_calculator_api.py
- tests/test_python_mt5_conversion_files.py
- summary/CODEX_SUMMARY_latest.txt

USDJPY merged-alert symbol routing

Root cause and behaviour implemented

- The default Bybit target sent an unambiguous forex entry such as USDJPY exclusively through the Bybit resolver, which produced the reported unresolved-symbol error before any alert save.
- Added the explicit resolver preference auto. It uses the existing shared OANDA-pair recognizer first, returning OANDA's canonical USD_JPY without a broker request; all other inputs continue through the existing Bybit path. Explicit bybit and oanda preferences are unchanged.
- The merged-alert form now resolves once before constructing its single save request. An auto-routed forex alert is posted only to OANDA, then switches the target and refreshes OANDA status, settings, and alert list through the existing monitor-change lifecycle, which invalidates stale Bybit responses. Form values remain intact while resolution/save is pending and errors identify the relevant market path.
- Existing expiry, Brisbane active-period, enabled, movement-window, and notification behavior is carried unchanged in the routed payload. BTC still resolves and saves to Bybit as BTCUSDT; BRUSDT and OPUSDT remain Bybit candidates rather than forex.

Focused verification

- PASS `python -m pytest -q tests/test_merged_alerts_runtime.py::test_merged_alerts_runtime_expiry_default_round_trip_and_notification_channels tests/test_master_service_symbol_resolution.py::test_auto_symbol_resolution_routes_unambiguous_fx_to_oanda` (2 passed in 9.69s).
- The Node/VM runtime coverage verifies a single USDJPY resolution, one canonical USD_JPY OANDA POST with preserved expiry/schedule/enabled data, no canonical forex Bybit POST, the OANDA view/list/status/settings refresh, and unchanged BTC Bybit routing. The server test covers USDJPY spellings plus BRUSDT/OPUSDT protection with mocked Bybit resolution.
- No live monitor, broker, exchange, notification, Render, TradingView, or production-data request ran. Phase 7 remains pending.

Exact files changed

- render/master_service.py
- render/static/merged_alerts.js
- tests/test_master_service_symbol_resolution.py
- tests/test_merged_alerts_runtime.py
- summary/CODEX_SUMMARY_latest.txt

Per-alert Brisbane weekend active window

Behaviour implemented

- Added active_period to Bybit and OANDA custom alerts with strict values anytime (default) and weekend_brisbane. Missing legacy values behave as Anytime; upserts and backup/restore coercion preserve the setting, while unknown values are rejected.
- Weekend only is evaluated strictly in Australia/Brisbane: Saturday 07:00 inclusive through Monday 07:00 exclusive. Inactive, disabled, and expired alerts are excluded before history, state, cooldown, firing, auto-removal, Telegram, or email handling.
- Each new active weekend resets the scheduled alert’s armed/cooldown state for that window. Weekend movement calculations filter out pre-07:00/pre-weekend samples, preventing a Friday or pre-window comparison from triggering after the window opens.
- Added the shared Active period form dropdown, Save/Update payload field, edit/reset/monitor-switch handling, and row display. Existing movement windows and all expiry/notification behavior remain intact.

Focused verification

- PASS python -m pytest -q tests/test_merged_alerts_runtime.py::test_merged_alerts_runtime_expiry_default_round_trip_and_notification_channels tests/test_merged_monitor_status_env.py::test_weekend_custom_alert_schedule_coercion_boundaries_and_state_isolation — 3 passed in 6.44s (one JavaScript runtime node plus two parametrised backend cases).
- No live monitor, broker, exchange, notification, browser, or production-data operation ran.

Exact files changed

- bybit_monitor/bybit_altcoin_monitor.py
- oanda_monitor/oanda_forex_monitor.py
- render/static/merged_alerts.js
- tests/test_merged_alerts_runtime.py
- tests/test_merged_monitor_status_env.py
- summary/CODEX_SUMMARY_latest.txt

Alert expiry preset dropdown

Behaviour implemented

- Replaced the shared alerts UI manual expiry mode and datetime-local control with one Expiry preset dropdown: Lifetime, 1 hour, 4 hours, 1 day, 1 week, and 1 month. Lifetime is the default and omits expires_at.
- Expiries are calculated only when Save or Update is clicked, then supplied as absolute UTC ISO-8601 values to the existing Bybit/OANDA shared backend. Hour/day/week selections use exact minute durations; one month keeps the local clock time and clamps end-of-month dates to the final valid target-month day.
- Editing an expiring alert adds a temporary locally displayed Keep current expiry option that preserves the exact stored ISO value, including expired alerts. Selecting a new preset restarts from Update; Lifetime clears expiry. Reset and monitor changes rebuild the base choices, preventing temporary-option accumulation.
- Existing expiry display, Expired state, backend enforcement, notification-channel behavior, symbols, alert kinds, movement settings, and all non-expiry alert fields are unchanged.

Focused verification

- PASS python -m pytest -q tests/test_merged_alerts_runtime.py::test_merged_alerts_runtime_expiry_default_round_trip_and_notification_channels (1 passed in 0.56s).
- The fixed-clock Node/VM test verifies all six choices, default/lack of manual control, Lifetime omission, four exact duration presets, end-of-month month clamping, future/expired exact preservation, replacement and clearing behavior, temporary-option cleanup on reset/monitor switch, and existing Telegram/email notification reporting.
- No live monitor, broker, browser, scanner, or production-data request was run.

Exact files changed

- render/static/merged_alerts.js
- tests/test_merged_alerts_runtime.py
- summary/CODEX_SUMMARY_latest.txt

ATR scanner Phase 1 corrective follow-up: cancellation lifecycle race and executable UI coverage

Correction

- Cancellation now records the exact task under _refresh_guard before cancelling it. start_refresh returns an explicit cancellation_in_progress non-started response until the cancelled task has terminated and that same task has been finalised. Finalisation verifies task identity before changing active settings, progress, or last result; the marker is cleared for cancellation, natural completion, and unexpected worker exceptions. Duplicate Stop requests are safe and do not finalise competing task state.
- Browser Stop now clears the auto-refresh timer before requesting cancellation, blocks Run and timer callbacks while Stop is pending, deduplicates Stop actions, restarts the configured full auto-refresh countdown after confirmed cancellation, and polls authoritative status plus restores scheduling after cancellation failure. Settings/Save/Reset remain available.
- The static JavaScript token test was replaced with a deterministic Node/VM fake-DOM, fake-fetch, and fake-timer behaviour test. It executes disabled-auto initialization/callback suppression, manual Run payload, active control state, Save-only behaviour, pending Stop deduplication/blocking, stopped status, and enabled auto-refresh timer replacement plus manual:false callback payload.

Focused verification

- PASS python -m pytest -q tests/test_atr_scanner_ui.py::test_atr_scanner_javascript_controls_manual_cancel_and_auto_refresh_behaviour (1 passed in 0.58s).
- The earlier deterministic service race test, tests/test_atr_scanner.py::test_cancellation_finalisation_blocks_competing_start_then_allows_latest_restart, had already passed and was intentionally not rerun in this continuation.
- The earlier UI attempts exposed only an assertion that counted the unrelated 2-second progress-status timer after the mock cancellation status sequence. The expectation was narrowly corrected to assert the required Stop state while the test continues to assert auto-timer suspension and replacement explicitly.
- No live scanner, service, browser, Bybit, account, or production-data request was run. No remaining blocker identified.

Exact files changed

- render/atr_scanner.py
- render/static/atr_scanner.js
- tests/test_atr_scanner.py
- tests/test_atr_scanner_ui.py
- summary/CODEX_SUMMARY_latest.txt

ATR scanner Phase 1: cancellation, manual runs, and auto-refresh control

Root cause

- Scanner settings saves and resets directly started manual refreshes, and the shared worker queued a second refresh whenever settings changed mid-scan. The page also disabled all action controls, had no cancellation operation, always scheduled browser auto refresh, and rendered old snapshots using newly saved settings.

Behaviour implemented

- Added persisted, strictly validated auto_refresh_enabled (default true for legacy settings) alongside the existing validated 30–3600 second interval. The runtime settings JSON was not read, modified, staged, or committed by this work.
- Settings save/reset now only persist and return settings. Active workers retain their immutable start snapshot; queued-settings refresh state was removed.
- Added Run scan, Stop scan, accessible auto-refresh switch, and editable interval controls. Settings and save/reset stay usable while scanning; Run/Stop swap availability based on worker state.
- Added local-only POST /api/atr-scanner/cancel. It cancels and awaits the active asyncio task without holding its cleanup lock, preserves the last good result, publishes a stopped progress outcome, is idle-safe, and permits a new run immediately after cancellation.
- Automatic browser requests are scheduled only when enabled; disabled automatic requests are also rejected by the service. Initial automatic scanning remains only when enabled. Saved/stop states restart the enabled countdown from the full configured interval.
- Results and basis now render from the snapshot settings that produced them, with an explicit next-scan notice when saved settings differ.

Exact files changed

- render/atr_scanner.py
- render/master_service.py
- render/static/atr_scanner.js
- tests/test_atr_scanner.py
- tests/test_atr_scanner_ui.py
- summary/CODEX_SUMMARY_latest.txt

Focused verification

- PASS tests/test_atr_scanner.py::test_settings_saved_during_refresh_does_not_queue_or_change_active_snapshot
- PASS tests/test_atr_scanner.py::test_cancelling_gated_worker_preserves_last_good_result_and_allows_restart
- PASS tests/test_atr_scanner_ui.py::test_scanner_page_has_required_controls_columns_help_and_accessibility
- PASS tests/test_atr_scanner_ui.py::test_scanner_api_settings_refresh_cancel_and_failure_codes
- PASS tests/test_atr_scanner_ui.py::test_atr_scanner_javascript_controls_manual_cancel_and_auto_refresh_contract
- One targeted pytest invocation only: 5 passed in 15.91s. No live scanner, browser, service, Bybit, or production-data request was run.

Delivery note

- Starting and final origin/master baseline: 3bdc2c32b40051b3ed54be88af964e0ea208f652 (unchanged). No genuine limitation identified.

Alert expiry/email, trade numbering/chart folders, funding totals/commission, and ZORAUSDT repair

Root causes and implementation

- Alert expiry did not exist end to end: the UI omitted it and neither monitor enforced it. The Alerts form now defaults to No expiry, converts browser-local date/time to UTC ISO-8601 expires_at, preserves it during edit/save/reload/backup/restore, displays No expiry/expiry/Expired, rejects malformed or past values, prevents re-enabling an expired alert, and excludes expired alerts from evaluation, re-arming, Telegram, and email while keeping them visible. Legacy alerts without expires_at remain non-expiring; fixed-price removal after firing is unchanged.
- Alerts previously had Telegram delivery only. Added the shared standard-library shared/alert_email.py SMTP transport, independent Telegram/email attempts for Bybit and OANDA custom price/move alerts and existing percentage-move alerts, per-channel readiness/test results, and backward-compatible /push-test behavior. Email messages include source, symbol, type, condition, current value, custom message when present, and UTC trigger time. No address, credential, or secret is committed or logged.
- Email configuration required when delivery is enabled: ALERT_EMAIL_ENABLED=true, ALERT_EMAIL_TO, ALERT_EMAIL_FROM, ALERT_SMTP_HOST, ALERT_SMTP_USERNAME, and ALERT_SMTP_PASSWORD; ALERT_SMTP_PORT defaults to 587 and ALERT_SMTP_USE_TLS defaults to true.
- Forward trade numbering/folder creation was absent from normal journal synchronization; the legacy backfill was not used. Canonical workbook logic now numbers only non-test row_type=trade rows, preserves all existing numbers, treats padded aliases as collisions, derives monotonic F/C sequences from workbook rows plus configured chart roots, uses open time then close time for the existing year/month hierarchy, creates folders under TRADING_JOURNAL_FOREX_ROOT/TRADING_JOURNAL_CRYPTO_ROOT, and performs deterministic allocation under the journal lock. Target Trade Number cells remain text and receive folder hyperlinks. Folder creation and workbook persistence are idempotent and atomic; failures cannot leave a workbook claiming an unlinked folder.
- The first real reconciliation validator failures were false positives caused by different openpyxl access paths materializing absent default-blank internal cells. Direct OOXML inspection proved AA1554, AA1555, AD1554, AD1555, AE1554, AE1555, AF1554, AF1555, AG1554, and AG1555 were absent and semantically empty/default before and after the targeted package patch. Presence is now considered equivalent only for a completely default blank cell with no value, formula, nondefault style, hyperlink, comment, merge significance, or other meaningful cell state. Meaningful differences still fail, and every non-target OOXML package part remains byte-protected.
- Open Bybit Demo/Live positions had no settled-funding aggregation. The service now identifies the current lifecycle from executions after the last flat state, preserves lifecycle through partial increases/reductions, distinguishes account/mode/symbol/currency/side/positionIdx, fetches paginated transaction logs in account/category batches with a short cache, applies the Bybit API sign convention (positive received, negative paid), and exposes Funding paid, Funding received, and positive-received Net funding columns. Retrieval or hedge attribution uncertainty produces a sanitized warning and an unknown dash, never a fabricated zero; order rows remain dashes.
- Crypto journal-import audit found one supported execution-history importer shared by Bybit Demo and Live; no Binance account-history importer exists. The Bybit CSV importer now reports trading fee total, funding paid, funding received, net funding, and funding cost separately. Commission is trading fees plus funding paid minus funding received. CSV Fees Paid uses positive cost/negative credit, unlike the transaction-log API. Net P/L remains gross P/L minus the combined Commission exactly once, preserving stable IDs, trailing-open deferral, legacy reconciliation, deduplication, and repeat-import idempotency. Diagnostics explicitly report that Bybit Demo/Live Commission includes funding.
- ZORAUSDT was not invalid. The metadata source remains the collection response from GET /fapi/v1/exchangeInfo, followed by exact PERPETUAL symbol selection. The prior public-request path exposed an opaque generic 502 and had no official-host transient failover. It now records only the safe endpoint, exception class, HTTP status, and Binance code/message; retries/failover occur only for connectivity, 429, and 5xx failures, never permanent 4xx failures. Filters are selected by filterType, so reordered entries work, and optional ticker/range/open-interest failures preserve partial specifications with warnings. The one post-fix live request on 2026-09-07 resolved ZORAUSDT as Binance USD-M PERPETUAL/TRADING, tick size 0.0000010, with no warnings; no regional restriction remained.

Reconciliation and preservation

- Foreground reconciliation completed successfully (state=completed, exit code 0) from 2026-09-07T03:13:20Z to 2026-09-07T04:08:24Z.
- Assigned and linked 134 historical non-test trades: F1030-F1160 (131 forex trades) and C374-C376 (3 crypto trades). Created the corresponding 134 folders in the established Dropbox roots and year/month layout.
- Candidate verification passed, 134 hyperlink relationships were added, and all non-target package parts were byte-verified unchanged.
- Minimal post-result OOXML verification found a valid ZIP, all 134 inline-text numbers, all 134 hyperlinks, and all 134 linked folders with no missing target.
- Final workbook SHA-256: 8A6BE01B4D5490F0CA13C7A28A09CEEDB02E3297EC0E9BAE262DD34DA7200BF0. This workbook content already matches the fetched HEAD blob, so no workbook diff requires staging.

Focused verification (no complete test files or suites)

- PASS tests/test_merged_alerts_runtime.py::test_merged_alerts_runtime_expiry_default_round_trip_and_notification_channels
- PASS tests/test_merged_monitor_status_env.py::test_expired_custom_alerts_are_never_evaluated_rearmed_or_sent (Bybit/OANDA parameter cases)
- PASS tests/test_merged_monitor_status_env.py::test_telegram_and_email_delivery_are_independent (mocked delivery only)
- PASS tests/test_master_journal_workbook.py::test_trade_number_allocation_folder_creation_test_exclusion_and_incremental_idempotency
- PASS tests/test_open_orders_page.py::test_bybit_open_position_funding_uses_current_lifecycle_and_reports_unknown
- PASS tests/test_master_service_bybit_demo_sync.py::test_manual_import_bybit_funding_with_completed_trades_and_trailing_open
- PASS tests/test_master_service_instrument_specs.py::test_zorausdt_specs_parse_reordered_filters_preserve_partial_results_and_report_safe_failures
- PASS tests/test_master_journal_workbook.py::test_incremental_cell_signature_accepts_only_presence_only_default_blanks (1 passed in 6.38s)
- No real email was sent; email tests used mocked transport. No full suite, complete test file, duplicate passing test, smoke test, or broad integration check was run.

Delivery

- The one authorized fetch found origin/master equal to HEAD at 317a0814df36573288ef28299f16351903da915a; there were no intervening commits or filenames to integrate.
- Only authorized task paths are intended for the final commit. Runtime JSON, deleted brief files, and uncertain bybit_monitor/settings.json remain user-owned and unstaged.
- The exact final commit hash and successful normal push confirmation are reported in the delivery response because a commit cannot contain its own hash.

Trader EA Phase 1: one-shot named-trendline lifecycle and manual re-arm

- `Trader.mq5` now requires a positive `TrendlineArmGeneration` to arm a named trendline. The same unchanged name is manually re-armed only by increasing that generation.
- A terminal-global lifecycle key is stable across account server/login, symbol, magic number, strategy, and trendline name. It atomically reserves an arm before submission, preserves working/consumed state through EA/chart/terminal reloads, and fails closed on uncertain state.
- Accepted pending orders and their resulting positions are adopted on upgrade, then become consumed when absent; TP, SL, manual/broker closure, cancellation, deletion, expiry, rejection, and ambiguous acceptance cannot recreate an order. An active lifecycle blocks a re-arm from placing a concurrent second order.
- Lifecycle logging is transition-based (`armed`, `working`, `consumed`) and marks explicit subsequent arms as `event=manually_rearmed`; no tick-driven replacement of a working pending order occurs.

Focused verification

- PASS `python -m pytest -q tests/test_trader_trendline_one_shot.py::test_trendline_lifecycle_reserves_a_durable_account_symbol_magic_name_generation tests/test_trader_trendline_one_shot.py::test_trendline_completion_and_cancellation_paths_consume_before_any_new_submission tests/test_trader_trendline_one_shot.py::test_same_named_line_requires_an_explicit_new_generation_to_rearm tests/test_trader_trendline_one_shot.py::test_reinitialization_adopts_existing_work_and_fails_closed_without_a_new_arm` — 4 passed in 0.29s (only test invocation).
- No focused MetaEditor compiler command exists in this repository. No terminal, Pepperstone account, production data, or real order was accessed; compile/deployment remains a manual MT5 step.

Trader EA Phase 1 correction: lifecycle tracking, exact ownership, and durable recovery

- A working tagged trendline pending order is now recalculated on the original new-bar cadence and amended in place with `CTrade::OrderModify` for its current trendline entry, SL, TP, and recorded expiry. It never creates a second pending intent; a failed amendment keeps the last confirmed order and stops automatic replacement.
- Trendline orders carry a compact lifecycle comment containing the stable account-server/login, symbol, magic, strategy, line fingerprint and generation. The durable record stores that comment, exact pending ticket, expiry, working generation, and highest blocked generation. Pending adoption requires the exact comment/ticket; filled position ownership is matched through the original order's `ORDER_POSITION_ID` and `POSITION_IDENTIFIER`. Same-symbol/same-magic non-lifecycle orders only block placement and are never adopted, amended, or cancelled.
- The durable record is a locked `FILE_COMMON` runtime file named from the lifecycle fingerprint. It is read before the terminal-global cache, survives terminal-global expiry, is flushed before the cache is updated, and causes fail-closed disarming on corruption, contradiction, read, or write failure. Runtime state is not checked in.
- A higher `TrendlineArmGeneration` supplied while the exact lifecycle is pending or positioned is recorded as blocked and logged once as `active_rearm_rejected`; it cannot queue after completion. The user must enter a later higher generation after the active work has finished. Unidentifiable pre-correction working cache records are migrated as consumed, not adopted, to prevent accidental ownership of an ordinary same-magic order.
- Source property and runtime `EA_VERSION` are both `2.33`.

Focused verification

- PASS `python -m pytest -q tests/test_trader_trendline_one_shot.py::test_working_pending_tracks_new_bar_by_exact_ticket_without_a_second_order tests/test_trader_trendline_one_shot.py::test_only_exact_lifecycle_comment_ticket_and_position_identifier_are_owned tests/test_trader_trendline_one_shot.py::test_durable_common_file_restores_consumed_state_before_expiring_terminal_global_cache tests/test_trader_trendline_one_shot.py::test_active_rearm_is_consumed_not_queued_and_completion_cannot_resubmit` — 4 passed in 0.25s (one test invocation).
- `Get-Command MetaEditor64.exe` found no focused compiler command. No MT5 terminal, account, production data, or live order was accessed. Compile `Trader.mq5` and perform the normal non-live deployment/runtime journal review manually before use.

Trader EA Phase 1 correction 4: classify definite rejection by server retcode

- Baseline: `84f270ebe774b0b8aeaff4d738fd9e8eeda8984b`. Modified paths are `mt5-clone/MQL5/Experts/Trader.mq5`, `tests/test_trader_trendline_one_shot.py`, and this summary only; the final correction commit is reported in the delivery.
- `SubmitExactTrendlineReplacement` now consumes only when the current server `ResultRetcode()` is an explicit definite rejection and both `ResultOrder()` and exact lifecycle-order reconciliation are zero. `CTrade::BuyLimit`/`SellLimit`'s Boolean no longer gates that server-authoritative rejection classification; it remains required for accepted-result validation together with an accepted retcode, nonzero ticket, and exact prepared-term observation.
- Transient, unknown, contradictory, ticket-bearing, or visibly matched outcomes remain unresolved, with the existing durable `replacing` state, no automatic retry, and no automatic re-arm. The durable consumed transition still requires a later higher manual generation to re-arm. All v2.35 ownership, cancellation, parsing, persistence, and replacement protections remain intact. Source property and runtime `EA_VERSION` are synchronized at `2.36`.

Focused verification

- One pytest invocation: PASS `tests/test_trader_trendline_one_shot.py::test_strict_v3_parse_and_replacement_outcomes_fail_closed_without_retry` — 1 passed in 0.22s. Pytest emitted two cache-write warnings because `.pytest_cache` is not writable; they do not affect the result.
- The single availability check, `Get-Command MetaEditor64.exe -ErrorAction SilentlyContinue`, found no command, so compilation remains unavailable and unverified; no MetaEditor compile invocation was run.
- No MT5 terminal, broker, account, market data, production data, demo order, live order, or other live/production operation occurred. Remaining manual action is to compile `Trader.mq5` in MetaEditor, deploy through the normal non-live process, and review the runtime journal. Phase 2 was not started.

Trader EA Phase 2: explicit EMA bounce selection

- Baseline: `7ee8903989d1b6d167a416e233554d84822e716c`. Authorized changed paths are `mt5-clone/MQL5/Experts/Trader.mq5`, `tests/test_trader_ema_selection.py`, and this summary only.
- EMA Bounce now exposes `BounceReferenceEMA` with MT5-facing `Fast EMA` and `Slow EMA` choices and defaults to Slow EMA. In dual mode both configured EMA handles still determine trend from fast-versus-slow ordering, while one centralized selector applies the chosen EMA to the closed candle's above/below bounce-reference condition. In single mode the selector always returns the `TrendEMAPeriod` EMA and ignores the fast/slow choice.
- Required EMA periods are validated before handle creation and invalid parameters fail initialization clearly. Closed-candle/new-bar timing, countertrend candle direction, market execution, risk/SL/TP/Net-R, one-trade protection, trendline lifecycle, standard strategies, and spread export paths remain unchanged. Source property and runtime `EA_VERSION` are synchronized at `2.37`.

Focused verification and deployment

- One pytest invocation: PASS `tests/test_trader_ema_selection.py::test_dual_ema_bounce_reference_is_explicit_selectable_and_defaults_slow` and PASS `tests/test_trader_ema_selection.py::test_single_ema_mode_period_validation_version_and_execution_paths_are_preserved` — 2 passed in 0.24s. Two `.pytest_cache` access warnings did not affect results.
- The exact `C:\Program Files\Pepperstone MetaTrader 5\MetaEditor64.exe` compile returned process exit code 1, while its explicit log reported `0 errors, 0 warnings` and successful code generation. The source hash remained unchanged and the fresh 131778-byte EX5 was generated at `2026-09-09T09:40:52.2065964Z`.
- One targeted deployment copied only `Trader.mq5` and `Trader.ex5` to the specified Pepperstone Experts directory. Repository/deployed SHA-256 matches are MQ5 `0D53255CBEC0AD9BB9696BB95949F3323A138691F1D8E09D473F950B8E7E5806` and EX5 `CA19DB74AE61FE97C50CA2B2DE40FB2711078E0D94B8A842FB9D08CFBC0B38DA`.
- The verified prior deployment backup is `C:\Users\User\AppData\Local\Temp\codex-trader-v237-backup-20260909-194155672`. No MT5 terminal was launched and no live/demo account, broker, market data, or order operation was accessed. Phase 3 was not started.

Trader EA Phase 3: separate desktop Trader control window

- Baseline: `f4fd025bda598eb1424e9dae642be807def4bb8c`. Authorized changed paths are `mt5-clone/MQL5/Experts/Trader.mq5`, `mt5-clone/trader_control_window.py`, `tests/test_trader_control_window.py`, `tests/test_python_mt5_conversion_files.py`, and this summary only.
- Trader now defaults to desktop-control mode and launches a separate standard-library Tkinter window for its exact account-server/login, chart, symbol, and magic instance. The window shows EA/account/instance/readiness/freshness details and four vertical one-click buttons: MARKET ORDER, LIMIT ORDER, TRENDLINE, and EMA BOUNCE. Legacy automatic strategies and the Standard Market chart button remain available only when desktop controls are disabled.
- A versioned FILE_COMMON JSON protocol accepts only a fresh canonical UUIDv4 command for the exact instance. Python publishes commands atomically with a sibling temporary file and `os.replace`; the EA durably verifies a per-command consumption marker before dispatch, rejects replay/malformed/stale/mismatched commands, performs at most one broker submission, never retries automatically, and returns structured accepted/blocked/rejected/uncertain results. Missing/stale/mismatched/not-ready state, `OrdersEnabled=false`, and pending commands disable every button; the separate per-instance process guard prevents duplicate windows.
- Each action reuses the current validated Trader inputs and existing risk, SL, TP, AutoTP/Net-R, readiness, broker-side, observable-ticket, and one-trade protections. Market uses a fresh Bid/Ask; Limit is a one-shot fixed standard limit; Trendline resolves selected/configured/sole chart lines in that order and allocates one durable Phase 1 generation per click while passive new-bar maintenance remains enabled; EMA Bounce uses the Phase 2 selected current EMA as one fixed limit price and never chases it. Source property and runtime `EA_VERSION` are synchronized at `2.38`.

Focused verification, compilation, and deployment

- One pytest invocation passed all five authorized nodes: `tests/test_trader_control_window.py::test_window_writes_one_atomic_scoped_command_and_blocks_duplicate_pending_clicks`, `tests/test_trader_control_window.py::test_ea_validates_scope_freshness_consumes_before_dispatch_and_reports_structured_result`, `tests/test_trader_control_window.py::test_four_actions_reuse_current_inputs_and_one_attempt_trading_protections`, `tests/test_python_mt5_conversion_files.py::test_mql5_trader_standard_market_is_live_quote_anchored_and_terminal_token_gated`, and read-only `tests/test_trader_trendline_one_shot.py::test_strict_v3_parse_and_replacement_outcomes_fail_closed_without_retry` — 5 passed in 0.47s. Two `.pytest_cache` permission warnings did not affect results.
- The one exact `C:\Program Files\Pepperstone MetaTrader 5\MetaEditor64.exe` compile returned process exit code 1; its explicit log reported `0 errors, 0 warnings`. `Trader.mq5` remained hash-identical and the fresh 179182-byte `Trader.ex5` was generated at `2026-09-09T13:31:55.5064275Z` with SHA-256 `DF8EA5B9B0C590C1E7083165EE0E26B7F73B62D1849F1414B5166C95C54C401B`.
- One targeted deployment copied only the MQ5/EX5 pair to the specified Pepperstone Experts directory. Repository/deployed SHA-256 matches are MQ5 `FE1808AC111C4579530E53062578B49501596164F8F9CA948E004A5D2CD3C0F4` and EX5 `DF8EA5B9B0C590C1E7083165EE0E26B7F73B62D1849F1414B5166C95C54C401B`. The verified prior deployment backup is `C:\Users\User\AppData\Local\Temp\codex-trader-phase3-deploy-backup-20260909T133258609Z`.
- No MT5 terminal/window was launched, no broker/live/demo account, market data, or order operation occurred, and no credentials or execution tokens were written. Phase 4 was not started.

Trader EA Phase 3 correction: remove stale version test marker

- Baseline: `b809b5d20daf55a09e6c8784c51c75840b84dddb`. Removed the inactive test-only `2.36` marker from `Trader.mq5`; the genuine active source property and runtime `EA_VERSION` remain synchronized at `2.38`, with no Phase 3 functional change.
- Updated only the stale version checks inside `tests/test_trader_trendline_one_shot.py::test_strict_v3_parse_and_replacement_outcomes_fail_closed_without_retry`. Every lifecycle assertion remains intact, and the node now requires the genuine `2.38` declarations while rejecting the fake marker and obsolete `2.36` declarations.
- One pytest invocation ran only that node: PASS — 1 passed in 0.21s. Two `.pytest_cache` permission warnings did not affect the result.
- The single exact `C:\Program Files\Pepperstone MetaTrader 5\MetaEditor64.exe` compile returned process exit code 1; its log reported `0 errors, 0 warnings`. The MQ5 hash remained unchanged during compilation, and the fresh 180398-byte EX5 was generated at `2026-09-09T21:06:41.6024787Z`.
- One targeted redeployment copied only `Trader.mq5` and `Trader.ex5`. Repository/deployed SHA-256 matches are MQ5 `FB1989DDBE257B4F077EE7BE8CC0532ED4C658D061E837F35F9E47FFB8C6B616` and EX5 `7C406325703E69F3272605C12B31D02DE2C56E002DC39757EA38C6DACFA1D94F`. The verified prior deployment backup is `C:\Users\User\AppData\Local\Temp\codex-trader-phase3-marker-backup-20260909T210736112Z`.
- Exact changed repository paths are `mt5-clone/MQL5/Experts/Trader.mq5`, `tests/test_trader_trendline_one_shot.py`, and this summary. No MT5 terminal/window, broker/live/demo account, market data, or order operation was accessed. Phase 4 was not started.

Phase 4 of 11: Bybit-first crypto Instrument Lookup

- Approved baseline: `3fb212eed572dd012ea9e6c8784c51c75840b84dddb`. The final focused commit containing this summary is reported in the delivery because a commit cannot contain its own hash.
- Root cause: `_fetch_instrument_specs` routed all crypto preferences straight to Binance, while the existing Bybit resolver depended on a cached symbol universe, used an account-derived base URL, and converted upstream failures into apparent misses. The browser also omitted an explicit crypto preference and hid the actual market-data source.
- Crypto preferences (`bybit`, `crypto`, `perp`, `perpetual`) and non-FX automatic routing now query Bybit first through the existing canonical helpers. Exact-looking pairs use bounded category-priority instrument queries; bare bases use `baseCoin` queries with deterministic `USDT`, `USDC`, then `USD` preference; cached fuzzy/alias resolution remains secondary with at most one forced refresh pass. Exact matches retain the category actually found for ticker, turnover, range, and BTC-reference enrichment.
- Instrument Lookup uses only a validated production public Bybit origin derived from `BYBIT_PUBLIC_MARKET_BASE_URL`; the ATR scanner public-origin helper delegates to the same validation. No credentials or account selection are consulted. Validated `retCode=0` empty responses can establish absence, while transport/429/5xx, malformed shapes, nonzero retCodes, and incomplete category lookups raise a Bybit-specific error and never trigger Binance.
- Binance remains available for explicit `prefer=binance`, or as a labeled fallback only after definitive Bybit absence. Fallback responses retain `source=binance_usdm` and include `_source_notice`; the UI sends `prefer=bybit` for crypto, preserves `prefer=oanda` for FX, uses the returned public-spec symbol for the unchanged journal request/history, and displays Bybit/OANDA/Binance-fallback source text outside the ordinary metric grid. Optional Bybit ticker, turnover, range, and BTC-reference failures preserve identified Bybit specs with warnings.

Verification and scope

- One pytest invocation passed the four exact nodes: `tests/test_master_service_instrument_specs.py::test_crypto_specs_use_bybit_first_for_generic_symbol_variants`, `tests/test_master_service_instrument_specs.py::test_crypto_specs_fall_back_to_labeled_binance_only_after_confirmed_bybit_absence`, `tests/test_master_service_instrument_specs.py::test_bybit_lookup_failure_never_silently_falls_back_to_binance`, and `tests/test_dashboard_home_layout.py::test_instrument_lookup_requests_bybit_and_labels_market_data_source` — 4 passed in 9.34s. Six existing FastAPI deprecation/pytest-cache warnings did not affect the result.
- The one authorized public-data command made exactly two credential-free GETs to `/v5/market/instruments-info`, with no retry: `ZORAUSDT` is currently listed in `linear`; `UAIUSDT` is currently listed in `linear`.
- Exact changed files are `render/master_service.py`, `render/static/instrument_lookup.js`, `tests/test_master_service_instrument_specs.py`, `tests/test_dashboard_home_layout.py`, and this summary. Production code contains no ZORA/UAI exceptions. No credentials, private endpoint, account, wallet, position, order, trading, MT5, deployment, or journal operation occurred. Phase 5 was not started.

Phase 5 of 11: canonical symbol resolution for saved alerts

- Approved baseline: `75328b2039026a45a938f6ba4087155f3d4369b0`. The final focused commit containing this summary is reported in the delivery because a commit cannot contain its own hash.
- Root cause: the shared alert resolver used an account-derived Bybit URL plus fuzzy cached-universe matching, the Bybit POST endpoint did not independently verify the submitted symbol before persistence, and monitor restore/startup paths could rewrite or delete legacy rows when symbol data was stale or unavailable.
- Linear alert resolution now uses the validated credential-free public Bybit origin and exact `/v5/market/instruments-info` metadata. Exact-looking symbols are queried directly; bare bases use `baseCoin`; only one active `Trading` `LinearPerpetual` with exact base/symbol and USDT quote/settlement is accepted. FX automatic routing remains OANDA, while absence/unsupported contracts produce a client error and transport/API/malformed-response failures remain distinct 502 errors. Alert persistence has no Binance fallback or fuzzy starts-with/contains path.
- The Bybit alert POST re-resolves every raw or client-canonical symbol server-side, merges edits without losing unchanged settings, preserves ID/`created_at` and every unrelated row in place, and writes under a mutation lock with verified alert-file/manifest snapshots and rollback on write, monitor-mirror, or round-trip failure. Legacy shorthand is migrated field-for-field only after successful exact verification; unavailable or unsupported rows remain unchanged and are displayed as unresolved. Non-strict restore and monitor startup no longer discard unresolved alerts.
- The browser requests `prefer=auto&scope=linear`, submits only the resolved symbol, treats the canonical alert returned by POST as display authority, shows `Resolved <input> to <canonical>` when changed, preserves form values on failure, and keeps FX routing and all existing alert settings. Monitor coercion is exact-only and canonical saved symbols remain the keys used by the existing price-trigger path, including fixed-price removal after firing.

Verification and scope

- Pytest invocation 1 ran the four exact nodes: PASS `tests/test_master_service_symbol_resolution.py::test_alert_resolution_maps_exact_bybit_linear_base_to_canonical_symbol`; PASS `tests/test_master_service_state_sync.py::test_bybit_alert_upsert_persists_only_server_verified_canonical_symbol_atomically`; PASS `tests/test_merged_alerts_runtime.py::test_merged_alerts_normalizes_ake_to_bybit_canonical_symbol_before_display`; initial assertion failure in `tests/test_merged_monitor_status_env.py::test_canonical_bybit_alert_symbol_matches_monitor_prices_and_fires` because the test expected runtime state to remain after the preserved fixed-price auto-removal path. Pytest invocation 2 reran only that failed node after correcting the expectation: PASS. Final targeted result: all four nodes passed. Existing FastAPI deprecation and unwritable `.pytest_cache` warnings did not affect results.
- The single authorized credential-free public GET returned `retCode=0`: `AKEUSDT` is currently listed in category `linear`, status `Trading`, contract type `LinearPerpetual`, quoted and settled in `USDT`, with `deliveryTime=0`.
- Exact changed files are `render/master_service.py`, `render/static/merged_alerts.js`, `bybit_monitor/bybit_altcoin_monitor.py`, `tests/test_master_service_symbol_resolution.py`, `tests/test_master_service_state_sync.py`, `tests/test_merged_alerts_runtime.py`, `tests/test_merged_monitor_status_env.py`, and this summary. Production code contains no AKE exception. Real alert/settings/journal files and all unrelated user-owned changes remained untouched. No alert, notification, email, credential, private Bybit endpoint, account, wallet, position, order, trading, MT5, or deployment operation occurred. Phase 6 was not started.

Phase 5 correction: preserve Lifetime expiry contract after symbol normalization

- Correction baseline: `65607f158c8fce72588fff8f34996bb58281c0e8`. The final focused commit containing this summary is reported in the delivery because a commit cannot contain its own hash.
- Restored the established browser payload contract: a new Lifetime alert omits `expires_at`; new finite alerts send the calculated ISO timestamp; existing Keep-current edits send their stored expiry; and only an existing alert with an expiry changed to Lifetime sends `expires_at: null` to clear it explicitly. Empty custom messages still clear on edit, and the canonical server alert remains required as the display authority.
- Updated the directly affected runtime POST mock to return the real `{ok: true, alert: ...}` endpoint shape, including an ID and canonical saved payload. The focused expiry test still checks the new-Lifetime omission and now verifies the edit-to-Lifetime null request plus expiry-free returned/displayed alert.
- One pytest invocation: PASS `tests/test_merged_alerts_runtime.py::test_merged_alerts_runtime_expiry_default_round_trip_and_notification_channels` and PASS `tests/test_merged_alerts_runtime.py::test_merged_alerts_normalizes_ake_to_bybit_canonical_symbol_before_display` — 2 passed in 1.26s. Two unwritable `.pytest_cache` warnings did not affect results.
- Exact changed files are `render/static/merged_alerts.js`, `tests/test_merged_alerts_runtime.py`, and this summary. No real alert, public-data, account, credential, trading, order, MT5, or journal operation occurred; pre-existing user-owned runtime files remained untouched. Phase 6 was not started.

Phase 6 of 11: automatic FX routing in the position calculator

- Approved baseline: `f951973f4e20fe85291adf1b332f50b03e7b6134`. The final focused commit containing this summary is reported in the delivery because a commit cannot contain its own hash.
- Root cause: both calculator endpoints trusted the browser's initial `asset=crypto` state and entered the Bybit path before applying the shared strict FX-pair classifier. The browser also retained its Crypto/Bybit state after canonical resolution and did not treat quote identity as server-authoritative during a resolution race.
- A single server routing helper now applies `is_likely_oanda_pair` and `normalize_oanda_symbol_query` before any Bybit dependency in both `/api/calculator/instrument` and `/api/calculator/quote`. Strict FX spellings received from stale/default Crypto route to effective `asset=fx`, `broker=oanda`, and the canonical OANDA symbol; explicit FX/Pepperstone remains Pepperstone. Instrument and quote responses now include the effective asset and broker, and the routed identity flows through quote diagnostics, calculation contexts, pending-webhook metadata, and response data.
- The browser adopts the returned effective identity before journal/specification requests or Crypto prewarming, synchronizes visible asset/broker/risk controls, stops the Crypto prewarm schedule, and stores the canonical symbol. Quote responses perform the same reconciliation before rendering or enabling Submit, so a delayed resolution race cannot leave subsequent submit, webhook, trendline-plan, or Pepperstone-export state as Crypto/Bybit. Controller and current-input checks prevent a stale response for an older symbol from changing current state.
- Exact targeted command: `python -m pytest -q tests/test_master_service_calculator_api.py::test_usdjpy_default_crypto_calculator_paths_auto_route_to_oanda_without_bybit tests/test_calculator_js_behavior.py::test_usdjpy_default_crypto_input_auto_selects_fx_and_posts_oanda_quote tests/test_calculator_js_behavior.py::test_usdjpy_quote_response_reconciles_fx_identity_after_resolution_race` — PASS, 3 passed in 13.66s in one invocation. Four existing FastAPI deprecation warnings and two unwritable `.pytest_cache` warnings did not affect the result.
- Exact changed paths are `render/master_service.py`, `render/static/calculator.js`, `tests/test_master_service_calculator_api.py`, `tests/test_calculator_js_behavior.py`, and this summary. All broker and browser dependencies were mocked; no real OANDA/Bybit credential, account, market data, wallet, position, order, alert, notification, server, browser, or runtime-state operation occurred. The pre-existing journal modification and every other user-owned change remained untouched. Phase 7 was not started.

Phase 7 of 11: repair OANDA spreads page startup

- Required starting baseline `65d3bf7c695d964208da62c59a343c4ee5398418` was verified for the current `master`, `HEAD`, local `master`, and `origin/master` before editing.
- Root cause: managed children were launched through a PATH-dependent `python`; process liveness was treated as readiness; and the shared launcher accepted any HTTP 200, including another launcher response returned by the proxy while the child socket was unavailable. This allowed premature recursive redirects and obscured startup failures.
- Managed web children now use the master's exact `sys.executable`. The shared proxy marks only a real upstream response as ready, coalesces repeated cold-start polling onto one startup task/process/port, preserves the canonical trailing-slash route and query string, and never exposes the child port. The launcher uses bounded readiness polling, redirects only after the upstream marker is observed, and surfaces sanitized child failure or timeout details with working Retry and Open logs actions.
- The OANDA spreads page continues to boot independently under `APP_BASE_PATH=/apps/spreads-clone`; its status and refresh URLs remain proxy-prefixed, and its existing initial background refresh stays nonblocking. Simulated refresh failure remains visible and manually retryable without broker access or real cache/state writes.
- Focused pytest command: `python -m pytest -q --tb=short tests/test_spread_monitor_dashboard.py::test_oanda_spreads_launcher_waits_for_real_upstream_and_surfaces_child_failure tests/test_spread_monitor_dashboard.py::test_oanda_spreads_page_boots_under_proxy_prefix_without_broker_io tests/test_spread_monitor_dashboard.py::test_spread_refresh_endpoint_starts_background_job_without_blocking`. Initial result: the two page/refresh nodes passed and the launcher node exposed a test-harness receive-channel defect. The first failed-node-only rerun exposed a redirect-count assertion defect. After the user-authorized correction, the exact additional command `python -m pytest -q --tb=short tests/test_spread_monitor_dashboard.py::test_oanda_spreads_launcher_waits_for_real_upstream_and_surfaces_child_failure` passed (`1 passed, 6 warnings in 6.23s`). All three authorized behaviors therefore pass; no passing node was rerun.
- Syntax command: `python -m py_compile render/master_service.py` completed successfully with exit code 0. Scoped `git diff --check` also passed.
- Exact changed paths are `render/master_service.py`, `tests/test_spread_monitor_dashboard.py`, and this summary. The pre-existing journal modification and all listed user-owned deletions/runtime-state changes remained untouched and unstaged.
- No real browser, Local Trading Tools stack, OANDA/Bybit endpoint, broker, account, credential, order, trade, notification, MT5, cache, or runtime-state operation occurred. Residual verification is intentionally limited to mocked process/proxy/browser behavior; no live startup was performed. Phase 8 was not started.
