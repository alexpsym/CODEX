Oanda Volatility tool and Pepperstone/MT5 ATR retirement

- Added `render/oanda_volatility.py`, a separate local managed web tool that discovers active OANDA `CURRENCY` instruments and calculates Wilder ATR(14) percentages from completed mid candles for 1m, 5m, 1h, 1D, 1W and 1Mo.
- Added bounded concurrent requests, completed-candle-aware caching, background refresh progress, deterministic ATR/name sorting, five-decimal percentage display, and per-timeframe N/A/error/stale diagnostics without mixing OANDA data with Pepperstone/MT5 prices.
- Added the exact `Oanda Volatility` dashboard button beside `Oanda Spreads`, the canonical `/oanda-volatility` route, a unique `oanda-volatility` managed process, click-only startup, and targeted shutdown handling.
- Removed the Pepperstone/MT5 ATR window, test, MQL5 source and ATR-only documentation. Removed the exact deployed `MarketWatchATRPercentFeed.mq5` and `.ex5` copies from the configured Pepperstone terminal Experts directory; no matching ATR desktop process was running.
- Preserved the MT5 Market Spread feed/window, Oanda Spreads, Bybit Scanner and Alerts. Updated one mixed MT5 preservation test so it no longer reads the retired ATR window.
- Targeted verification only: 4 exact pytest nodes passed; `py_compile` passed for the two changed Python runtime files; `node --check render/static/dashboard.js` passed; `git diff --check` passed; the narrow retired-identifier scan returned no matches.
- A live OANDA request was not performed because network access is restricted; no broad tests or service smoke checks were run.
