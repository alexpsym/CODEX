Completed the Spread Monitor broker separation, OANDA refresh resilience, Pepperstone export/import visibility, and Trading Journal workbook repairs.

Changes made:
- Updated spreads-clone/spread_core.py:
  - Added broker-aware cache sanitizing so OANDA payloads cannot show legacy Pepperstone/MT5 warnings, errors, or records.
  - Added configurable refresh interval via SPREAD_MONITOR_REFRESH_SECONDS.
  - Added per-request OANDA timeout, global refresh timeout, bounded concurrency, progress diagnostics, and timed_out handling for stuck refreshes.
  - Added stale fallback warnings when cached OANDA rows are shown after a failed or timed-out refresh.
- Updated spreads-clone/spread_app.py:
  - OANDA selection now starts a fresh OANDA refresh and hides stale cache rows until fresh data arrives.
  - Added clearer in-progress/timeout messaging and progress text from refresh diagnostics.
  - Kept Pepperstone display/import separate from OANDA refresh.
- Updated spreads-clone/oanda_spreads.py and spreads-clone/symbols.py:
  - OANDA requests now honor the configured per-request timeout.
  - Symbol universe no longer appends every available OANDA instrument by default; it uses env symbols, journal/watchlist symbols, and filtered defaults unless explicitly opted in.
- Updated spreads-clone/pepperstone_import.py:
  - Missing export errors now show the exact repo import path, the MT5 fallback MQL5\Files path, and the copy script instruction.
- Updated mt5-clone/MQL5/Experts/Trader.mq5:
  - Added EA version logging, startup export diagnostics, immediate forced Pepperstone spread export on init, symbol counts, and exact requested/fallback export paths.
- Updated mt5-clone/copy_pepperstone_spreads_latest.bat:
  - Accepts an explicit MT5 data path or MT5_DATA_PATH.
  - Auto-detects the newest MetaQuotes terminal export in APPDATA/LOCALAPPDATA when no path is provided.
  - Copies to mt5-clone\pepperstone_spreads_latest.json and prints source/target paths.
- Updated tools/master_journal_workbook.py:
  - Reworked Net P/L % to use capital-weighted account returns instead of summing trade percentages.
  - Converts AUD and USD/USDT/USDC accounts to AUD for overall percentage returns using JOURNAL_USDT_TO_AUD_FALLBACK.
  - Splits STATS1/report streak rows into count-only rows plus Start and End rows.
  - Preserves manual dashboard merges when inserting optional source/detail rows.
  - Populates SYMBOLS latest timeframe, stop, and target columns dynamically while preserving grouped headers and vertical single-header merges.
  - Repairs STATS2 As Of column values/styles from STATS1!B146.
- Updated tests for the new Spread Monitor behavior, OANDA symbol universe, workbook weighted percentages, streak Start/End rows, SYMBOLS latest columns, and STATS2 formatting.

Verification run:
- python -m pytest tests/test_spread_core.py tests/test_oanda_spread_fetch.py tests/test_spread_monitor_dashboard.py tests/test_pepperstone_spread_import.py tests/test_mt5_spread_fetch.py tests/test_python_mt5_conversion_files.py -q --basetemp .pytest_tmp_codex_spreadreq
  Result: 48 passed, 8 existing FastAPI deprecation warnings.
- python -m pytest tests/test_master_journal_workbook.py tests/test_master_service_journal_validation.py -q --basetemp .pytest_tmp_codex_journalreq2
  Result: 130 passed.
- python -m compileall spreads-clone render tools tests
  Result: passed.

Notes:
- The pytest commands used workspace --basetemp directories because the default Windows temp pytest root hit a permission issue earlier in the run.
- Existing unrelated dirty files were left untouched: custom instructions/CODEX_BRIEF_INSTRUCTIONS.txt and render/data runtime state JSON files.
