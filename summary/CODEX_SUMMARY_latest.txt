CODEX FOLLOW-UP SUMMARY
Brief: BRIEF 2 FOLLOW-UP — TARGETED DEFECT REPAIR AND BYBIT DEMO CREDENTIAL CLEANUP
Recorded: 2026-08-24 (Australia/Brisbane)
Status: targeted repository repairs complete and verified; Process-scope credential cleanup remains blocked by the active workspace safety policy

BASELINE AND PRESERVATION

- Resumed branch codex/journal-equity-fx-final-20260726 at HEAD ffc2676e29f8465c36b789896d60b1fcefdb4540, matching origin/master.
- Read repository-root AGENTS.md, the complete original CODEX_BRIEF_2026-08-20_Trading_Tools_Brokers_Pine_FXWeekend.txt, and the prior summary before editing.
- Preserved the five pre-existing runtime changes under render/data/: monthly_aud_revaluation.json, monthly_aud_revaluation_state.json, pending_webhooks.json, trade_contexts.json, and webhook_attempts.json.
- Did not inspect or modify custom-instruction files, C:\GPT\env.env, credentials, runtime state, or unrelated components.
- Trading Journal.xlsx remained byte-for-byte unchanged. SHA-256 before and after: BAAAC1C2F0B51BF1372F8804989325DC972CC3CB4490D3AE77E2A9331509BA17.
- No order, cancellation, position closure, liquidation, or other broker mutation was performed.

IMPLEMENTED REPAIRS

1. FX Weekend news cutoff evidence

- fxweekend-clone/liquidate.py now derives news cutoff success from the authoritative last_verified_flat_at timestamp recorded after each selected account's zero-open-items verification.
- Added strict news-only parsing that accepts timezone-aware datetime/ISO evidence, converts it to Australia/Brisbane, and rejects missing, malformed, or timezone-naive values. The legacy weekend timestamp parser was intentionally left unchanged.
- Every selected account must exist in the result, report state=verified flat, and provide valid timezone-aware verification evidence. The latest selected-account timestamp is the persisted verified-flat time and determines whether all required accounts were flat by the cutoff.
- Removed the fallback that substituted the scheduler attempt timestamp for missing verification evidence.
- cutoff_met is true only when the attempt was not already late, all selected-account evidence is complete, the latest actual verification is no later than the cutoff, and neither cutoff_met=False nor a prior missed-cutoff state already exists.
- Late valid flat evidence remains recorded with cutoff_met=False. Missing/invalid evidence records no verified-flat timestamp, cannot become on-time success, and retains the existing bounded retry/post-release completion behavior. Previously missed guarantees remain missed after later closure.
- Existing selected-account scope, account-wide news liquidation, overlapping-event coalescing, retry/backoff, deletion audit, persistence, and weekend paths were not redesigned.

2. Pine session marker timing

- pinescripts/custom_indicator.pine retains the end-inclusive event boundary time < eventTs and eventTs <= time_close, session-local timezone/weekday filtering, xloc.bar_time positioning, and the <=30-minute visibility gate.
- Session objects are now created only when the containing bar is confirmed, instead of when it is new. Realtime 5-, 15-, and 30-minute bars therefore do not create a marker at the preceding candle's opening or mid-bar update.
- The confirmed end-inclusive bar still creates Friday's 17:00 America/New_York close at the actual event timestamp when no following market candle exists.
- One confirmed execution per containing bar preserves realtime deduplication. Label spacing, bounded marker arrays, visibility cleanup, and the moving 100-candle marker were unchanged.

3. Display character repair

- Replaced both FX Weekend news audit-table mojibake placeholders with valid UTF-8 em dashes (—).
- Replaced the FX Weekend save-progress mojibake with a valid UTF-8 ellipsis (…).
- Replaced the equity hover invalid-percentage mojibake with a valid UTF-8 em dash (—). The existing base percentage formatter already contained the correct em dash and remains covered by the regression.
- Narrow tests verify the template strings and the exact U+2014 runtime output from both equity percentage formatters.

BYBIT CREDENTIAL CLEANUP AND READ-ONLY VERIFICATION

- The active workspace-level machine-safety instruction prohibits modifying environment variables. The user supplied an exception to AGENTS.md, but it cannot override that higher-priority restriction. No environment variable was changed, no process was terminated, and Local Trading Tools was not restarted.
- Secret-safe presence checks displayed booleans only. At check time, BYBIT_DEMO_API_KEY and BYBIT_DEMO_API_SECRET were already absent from Windows User scope but remained present in the PowerShell Process scope inherited by this Codex session.
- After the normal application environment bootstrap loaded C:\GPT\env.env without displaying its contents, sanitized readiness confirmed a complete Live KEY1 pair and a complete Demo KEY2 pair. Demo also detected the inherited complete DEMO_EXPLICIT pair, so the intended conflict guard stopped readiness.
- Bybit Live: PASS. Exactly one private read-only preflight endpoint was used: GET https://api.bybit.com/v5/user/query-api. It authenticated with source KEY1. Returned metadata reported readOnly=0 and one IP binding; therefore the credential is trading-capable, although this verification performed GET only.
- Bybit Demo: NOT RUN. The resolver detected differing complete KEY2 and DEMO_EXPLICIT sources and stopped before network access, so no Demo request was made and no credentials were sent to any endpoint.
- No secret value, key character, signature, bearer token, signed URL, or env-file content was displayed, logged, copied, or committed.

Remaining manual credential action:

The inherited Process-scope obsolete pair must be cleared from the shell/process that starts Local Trading Tools. The idempotent User-scope calls are included because existing long-lived processes may have inherited the old pair:

[Environment]::SetEnvironmentVariable('BYBIT_DEMO_API_KEY', $null, 'User')
[Environment]::SetEnvironmentVariable('BYBIT_DEMO_API_SECRET', $null, 'User')
Remove-Item Env:BYBIT_DEMO_API_KEY,Env:BYBIT_DEMO_API_SECRET -ErrorAction SilentlyContinue

After those commands, Local Trading Tools must be started/restarted only through the repository-owned run_local_master_control.bat launcher from that cleared shell. A subsequent Demo GET /v5/user/query-api preflight is still required to verify the KEY2 credentials. Until then, the overall environment-cleanup requirement and Demo authentication verification remain incomplete.

TARGETED VERIFICATION RESULTS

1. python -B -m pytest tests/test_fxweekend_news.py -q -p no:cacheprovider
   RESULT: 23 passed, 4 existing FastAPI deprecation warnings in 12.25s.
   Coverage includes exact-cutoff verification, cutoff-start/late-finish, one late account in a multi-account scope, missing/malformed/naive verification timestamps, prior-missed permanence, retries/coalescing, persistence/deletion, and corrected display strings.

2. python -B -m pytest tests/test_custom_indicator_pine.py -q -p no:cacheprovider
   FINAL RESULT: 29 passed in 0.39s.
   The focused timing model covers 5-, 15-, and 30-minute realtime updates and the final Friday New York close without a following bar. The final run followed a test-only strengthening of the Friday model.

3. python -B -m pytest tests/test_trading_journal_equity_curve.py::test_invalid_percentage_placeholders_are_single_utf8_em_dashes -q -p no:cacheprovider
   RESULT: 1 passed in 1.13s.

4. node --check render/static/trading_journal_equity_curve.js
   RESULT: PASS.

5. In-memory compile() of every changed Python file (fxweekend-clone/liquidate.py and the three changed Python test files)
   RESULT: PASS; no bytecode/cache output requested.

6. git diff --check
   RESULT: PASS. Git emitted only the repository's existing LF-to-CRLF working-copy notices.

7. Targeted mojibake scan of fxweekend-clone/liquidate.py and render/static/trading_journal_equity_curve.js
   RESULT: PASS; no remaining â/Ã sequences were found in those files.

No full Brief 2 matrix, Open Orders suite, calculator suite, broker service suite, MQL compilation, workbook suite, browser smoke test, or external TradingView compilation was run. This follows the follow-up's targeted verification/credit-control requirement. Pine verification for this repair is static/regression-based only.

CHANGED TASK FILES

- fxweekend-clone/liquidate.py
- pinescripts/custom_indicator.pine
- render/static/trading_journal_equity_curve.js
- tests/test_fxweekend_news.py
- tests/test_custom_indicator_pine.py
- tests/test_trading_journal_equity_curve.py
- summary/CODEX_SUMMARY_latest.txt

PATCH_DESKTOP_PUSH.bat is required immediately after this summary is written. The final assistant response will report the script's actual result and independent local/remote HEAD comparison; this summary does not pre-claim that outcome.
