Instrument Lookup request handling and latency follow-up

- Corrected `_fetch_instrument_specs()` so Binance HTTP failures are translated only inside Binance requests and OANDA HTTP failures return an accurate OANDA-specific 502 message. Successful FX routing and lookup behavior are unchanged.
- Refactored only the Binance specification path to create one request-scoped `httpx.AsyncClient` and share a conservative four-request semaphore across exchange information, requested-symbol data, and BTC-reference data.
- Requested-symbol and BTC-reference market snapshots, seven-day turnover, and exact-interval range requests now run concurrently under that shared bound instead of serially creating a new client for every request.
- Preserved successful-response caches, stale exchange-info fallback, non-caching of failures, warning collection, normalization, filterType parsing, response fields, BTC reference behavior, `(high - low) / open`, the one-candle lookback, and all exact Binance intervals.
- Journal independence, frontend code, UI layout, workbook, dashboard, and calculator were not changed.
- Added one narrow OANDA regression proving an OANDA `httpx` failure is not labeled as Binance. Updated the existing Binance interval/cache fixture only to accept the request-scoped client arguments.
- Targeted verification only: the exact OANDA error test and exact Binance interval/cache-key test passed once (`2 passed`). No full test file or suite was run.
- Focused local route observations: ZECUSDT returned 200 in 1.85s with source `binance_usdm`, nine ranges, and a BTC reference; BTCUSDT returned 200 in 0.81s with source `binance_usdm`, nine ranges, and no self-reference.
- Changed only `render/master_service.py`, `tests/test_master_service_instrument_specs.py`, and this summary.
