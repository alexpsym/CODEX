Final implementation summary - Revision 41 import balance scoping and rollback reporting

Publication status
- Work was performed only in C:\GPT\CODEX-master from the current local checkout.
- This summary is written before publication as required by AGENTS.md.
- PATCH_DESKTOP_PUSH.bat must run next. The task is not complete until it succeeds and verifies local HEAD equals origin/master.

1. Non-OANDA Excel import compatibility
- Added _is_oanda_transaction_export_balance(), which independently checks source and balance_source for the exact oanda_transaction_export_balance provenance token.
- _import_uploaded_trading_journal_file() now derives one scoped OANDA transaction-export balance and uses only that value for the pending-balance bridge, pre-sync exact verification, workbook Account Balances reread verification, forced snapshot verification, and rollback balance verification.
- The original parsed balance remains available for balance_parsed diagnostics and the existing balance-only rejection.
- Generic local_excel/excel balances no longer become OANDA-style authoritative seeds and no longer block otherwise valid imports when they are absent from the authoritative timeline.
- Generic uploads retain their existing parsing, P/L inference, upsert, master-workbook sync, row-ID verification, SQLite persistence, and GitHub sync workflow.
- balance_applied and snapshot_visible remain false for generic parsed balances that were not durably and exactly verified; balance_parsed remains true.
- The Pepperstone MT5 Excel import succeeds, persists its row, keeps balance_after_trade 1008.5, and calculates net_profit 8.5.
- A second ordinary non-OANDA Excel workbook with a parsed account balance also succeeds with balance_parsed:true and balance_applied:false.

2. GitHub rollback truthfulness
- GitHub sync tracking now distinguishes a call that started from a call that returned a result.
- Sync never started: remote state is reported unaffected/restored.
- Sync returned and explicitly reported disabled: remote state is reported unaffected/restored.
- Enabled sync returned failure: remote state is not reported restored.
- Sync raised before returning: remote state is reported false/unknown, not restored.
- rollback_local_state_restored now reports successful local journal/workbook/SQLite rollback independently.
- rollback_restored is true only when both local restoration and remote-state restoration are genuinely established.

3. Regression coverage
- The existing realistic master-journal regression still uses a temporary copy of Trading Journal.xlsx with one existing trade and cashflow row.
- Its OANDA export contains an opening ORDER_FILL, a closing ORDER_FILL at 1010.00, and a later DAILY_FINANCING balance at 1008.75.
- It exercises the real parser, authoritative importer, workbook generation/update, workbook reread, cache clearing, forced snapshot rebuild, SQLite persistence, and duplicate import.
- It verifies the stable row ID exists once, the final trade balance remains 1010.00, the authoritative account balance remains 1008.75 with OANDA provenance, prior trade/cashflow IDs survive, response state is truthful, and no separate OANDA workbook is created.
- New focused coverage checks both provenance fields independently, Pepperstone compatibility, a second ordinary Excel import, and an exception raised by GitHub sync after the new row/workbook state was locally committed.
- The raised-sync regression verifies local rollback succeeds while rollback_github_state_restored and rollback_restored both remain false.

4. Verification
- New focused importer/rollback regression slice: 4 passed.
- tests/test_master_service_oanda_history_export.py: 44 passed.
- OANDA-selected tests in tests/test_master_service_trading_journal_crud.py: 29 passed, 169 deselected.
- Pepperstone/non-OANDA/manual-import/duplicate/rollback selection in tests/test_master_service_trading_journal_crud.py: 38 passed, 160 deselected.
- Existing datetime serialization, failed-write/cache durability, real-workbook equity cache, and history-page UI/download selection: 11 passed.
- Additional OANDA AEST ordering and Brisbane local-time checks: 2 passed.
- python -m py_compile render/master_service.py tools/master_journal_workbook.py: passed.
- node --check render/static/history_page.js: passed.
- git diff --check: passed with only normal LF-to-CRLF working-copy notices.
- No full suite and no real broker request were run.
- Diagnostic note: one extra legacy manual-quality resync test was invoked while identifying the datetime slice. It has a pre-existing assertion for a Python datetime where the completed serialization behavior returns an ISO string. Neither that test nor tools/master_journal_workbook.py was changed for this task.

5. Safety and preservation
- journal/Trading Journal.xlsx had a pre-existing local modification and remained protected. Its SHA-256 stayed exactly 8925B34088B24CB8415A936B8277E6C3F4ABF952A9B8302DB0BF359A05BFED29.
- Pre-existing changes in render/data/monthly_aud_revaluation_state.json, pending_webhooks.json, trade_contexts.json, and webhook_attempts.json were not edited for this task and must remain unstaged/unpublished.
- journal/OANDA LIVE.xlsx and journal/OANDA DEMO.xlsx do not exist.
- The completed Pine, datetime serialization, cache durability, master-workbook balance provenance, and browser-download implementations were not changed.
- Repository-local pytest temporary directories are ignored and remain unstaged.
- No custom instructions file, secret, environment file, log, cache, or runtime data is included in the intended commit.

Changed task files
- render/master_service.py
- tests/test_master_service_trading_journal_crud.py
- summary/CODEX_SUMMARY_latest.txt

Mandatory completion step
- Run repository-root PATCH_DESKTOP_PUSH.bat.
- Do not report completion unless it succeeds and proves local HEAD equals origin/master.
