CODEX COMPLETION SUMMARY
Brief: Reduce Unified Trader Market Watch CPU work and restore responsive ATR/spread updates.
Recorded: 2026-08-26 (Australia/Brisbane)

ROOT CAUSE CONFIRMED

- Trader.mq5 recalculated every configured ATR timeframe for each selected-symbol batch every second, including W1/MN1, and general Market Watch symbols consumed the four-symbol quota.
- It detected Forex with symbol-name parsing, shared its spread refresh with the heavy ATR timer, and rewrote the full JSON feed on every timer pass.
- It had no terminal-wide producer coordination, so every enabled instance repeated the scan and created duplicate ATR handles.

IMPLEMENTED

- The unified table/feed now contains only M1, M5, H1, and D1. The desktop table uses the same four columns.
- ATR values cache the last fully closed bar timestamp per symbol/timeframe. Existing indicator handles are reused; CopyBuffer/iClose run again only after that closed bar changes, or a bounded retry is due.
- Forex eligibility now uses SYMBOL_TRADE_CALC_MODE and a dedicated Forex worklist, so non-Forex symbols do not consume the batch quota.
- A lightweight 100 ms tick/timer spread path publishes only changed values. ATR batches remain limited to one per second, and unchanged feeds are not rewritten.
- A short terminal-global owner lease ensures one enabled Unified Trader instance produces the shared feed; a stale owner can be replaced and normal deinitialisation releases ownership and handles.

VERIFICATION

- Exact command: & 'C:\Program Files\Pepperstone MetaTrader 5\MetaEditor64.exe' /compile:'C:\GPT\CODEX-master\mt5-clone\MQL5\Experts\Trader.mq5' /log:'C:\GPT\CODEX-master\mt5-clone\Trader.compile.log'
- Result: 0 errors, 0 warnings, 8135 ms elapsed, cpu='X64 Regular'.

DELIVERY

- No test suite, backtest, or additional compile was run.
- Commit/push intentionally not run because the user explicitly prohibited them.
