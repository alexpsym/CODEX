Completed the Pine/session/spread/Pepperstone calculator fix.

Changes made:
- Updated pinescripts/custom_indicator.pine:
  - Removed ATR inputs, ta.atr calculation, and ATR plotting entirely.
  - Gated Volume, VWAP, VWAP offsets, funding lines, and option expiry lines to crypto charts only via syminfo.type == "crypto".
  - Added readable Funding and OP EX labels and bounded label arrays alongside line arrays.
  - Added separate session open/close visibility controls while preserving Sydney, Tokyo, London, and New York toggles.
  - Added session labels such as Sydney Open and London Close and bounded session labels to avoid object leaks.
  - Increased max_labels_count.
- Updated the spread monitor:
  - Treats spread_pct <= 0 or missing as unavailable in broker_cell and frontend rendering.
  - Ignores stale Pepperstone zero cached records instead of displaying 0.0000%.
  - Added MT5 initialize timeout handling using PEPPERSTONE_MT5_INIT_TIMEOUT_MS / MT5_INITIALIZE_TIMEOUT_MS, default 9000 ms.
  - Uses the timeout-aware initializer in preflight_mt5_environment(), available_mt5_symbols(), and fetch_mt5_spread_samples().
  - Returns the actionable Pepperstone auth message: MT5 authorization failed. Log into the Pepperstone MT5 terminal, then refresh.
  - Preserved OANDA spread behavior.
- Updated the calculator:
  - Added an FX-only Broker toggle with OANDA default and Pepperstone option.
  - Preserved the existing OANDA quote path by default.
  - Added /api/calculator/pepperstone-set for MT5 Expert Set downloads.
  - Pepperstone .set export is limit-order only; market .set generation is blocked with a clear message because Trader.mq5 has no safe one-shot manual market strategy.
  - Generated .set files use only Trader.mq5 input names and map StandardLimitSide 0 for buy limit and 1 for sell limit.
  - SL_DistancePoints equals calculator stop_loss_ticks; NetRR_Target equals calculator risk_reward.
  - Added a Download .set button after Pepperstone FX limit calculations.
- Updated tests:
  - Added static Pine regressions for ATR removal, crypto-only gates, timing labels, and session controls.
  - Added spread monitor regressions for zero/unavailable handling and stale Pepperstone zero cache records.
  - Added MT5 timeout/auth regression coverage.
  - Added calculator broker-toggle behavior and Pepperstone .set export tests.
  - Kept a compatibility fix so legacy EMA yes/no quality criteria still normalize as Yes/No while 9/20 EMA values remain supported.

Verification run:
- python -m pytest tests/test_calculator_js_behavior.py tests/test_calculator_js_syntax.py tests/test_master_service_calculator_api.py tests/test_spread_core.py tests/test_spread_monitor_dashboard.py tests/test_mt5_spread_fetch.py tests/test_custom_indicator_pine.py -q
  Result: 150 passed, 12 existing FastAPI deprecation warnings.

Notes:
- render/data runtime state files and state_backup.json were dirtied by the calculator tests; PATCH_DESKTOP_PUSH.bat excludes those paths from its staged commit.
- custom instructions/CODEX_BRIEF_INSTRUCTIONS.txt was already modified before this task. It was not changed for this fix and was temporarily excluded from the auto-commit.
